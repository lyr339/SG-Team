# 拾光 architecture

拾光 (SG Team) is an independent desktop control plane for Cursor multi-Agent teamwork. It does not run inside any Cursor extension and does not depend on any plugin process: channel messages, presence and Agent MCP all flow through the embedded SG Team server plus a shared SQLite database.

Dependency direction:

```text
renderer -> preload API -> Electron main (services)
                              |
                              v
                        SQLite (single database file)
                              ^
                              |
                    stdio Agent MCP (SG Team, per-channel identity)
```

Rules:

- SQLite is the only inter-process medium between the Electron main process and the MCP server processes; both open the same database file and rely on `BEGIN IMMEDIATE` + revision CAS for cross-process atomicity.
- The renderer is a pure snapshot consumer: it receives validated domain snapshots via IPC push/pull and never advances workflow state directly. Task/collaboration writes go through the Agent MCP tools (`team_task`, `team_message`, …); the desktop IPC surface exposes read projections plus workflow entry points (session pool creation and end, handoff, group membership, account switching) and exactly one task write, `planTeamGroupTasks` — the user planning tasks into a collaboration group, on the same aggregate path as `team_task plan` (decision D2: planning authority in a lead-less group belongs to the user first).
- Snapshot assembly is layered once, in order: `ChannelMessageRelay` owns conversations/presence for the embedded channels and orders entries by when they entered the dialogue; `LocalSessionBridge` is the transport that merges relay data into the base snapshot; `DesktopSessionService` adds Cursor telemetry and the live process projection on top and seals finished native turns onto persisted replies. The service reacts to transport events once and coalesces pushes into a microtask, so one relay event yields one post-seal snapshot.
- The MCP surface is nine tools in one native `SG Team` entry: the communication contract is exactly two tools, `check_messages` / `record_reply` (`channel_id`, plus an optional per-seat `session` token, see below); the seven team tools are grouped by object (`team_check_in`, `team_tasks`, `team_task`, `team_review`, `team_message`, `team_memory`, `team_run`) with an `action` / `view` enum each (see `docs/TASK-MCP.md`). Cursor-native process events are observed directly and the keepalive marker is `<sg_team_keepalive n="N"/>`.
- Agent identity is `workspace hash + channel + install generation` (`workspaceId:ch-N:generation`); reinstalling a workspace revokes prior generations immediately, so stale MCP processes are fenced.
- Sessions on the same channel are fenced by a per-seat session token, not by waiting for old heartbeats to expire (see "Run modes and the session fence").
- Task, attempt, lease, review and agent-registration state is owned by application/domain services.
- Cursor integration is observation + automation, never a control dependency: CDP (session creation, stream observer, auto-heal) and `state.vscdb` reads degrade gracefully when Cursor is absent.
- Durable storage of raw Cursor/user conversation history remains deferred. Agent-to-Agent collaboration is separate: it is already persisted by `TeamRun + stable AgentSlot + messageId`, with notification, read and response receipts.

## Run modes: the session pool, collaboration groups and the session fence

A workspace has one active run at a time, and since phase 2 · 2B-1 the only run it can start is the session pool (`session-run:<workspace>:<key>`). One-shot team runs (`team-run:<workspace>:<key>`, `software-core-v1`, lead + members started and ended as a whole) can no longer be created, configured, launched or given a goal; the ones that exist are archived (`completed`) by the v9 migration and are read-only history — their tasks, messages and memory stay readable, their sessions are retired by the fence. Both kinds still live in the same `team_runs` table; `activeRun` is the newest run of the active workspace (an archived team run that is still the newest run makes the 运行 page show the start page with one explanatory line). `workspaceRunMode(run) === 'independent'` (`isSessionPoolRun`) is the only pool test in the code base; the `activeRun` semantics were left untouched on purpose (170 call sites).

### The independent batch is a session pool

The independent batch is long-lived: every Cursor session is one seat (`agent_slots`, channel = seat, one `session_token` each) that starts solo and only talks to the user. **Collaboration groups** (`team_groups`, schema v8) are contexts created and dissolved inside the pool at any time: a goal, members, per-member roles, an optional lead, and — through `group_id` snapshots — their own tasks, messages and run-scoped memory. Joining or leaving a group never touches the seat's token, Composer, `generation` or the run status; the pool run stays `running` for its whole life.

- **Membership has one source of truth**: `agent_slots.group_id` (plus `home_role_id` / `group_joined_at`, only set while grouped). `is_solo` flips with it, so every existing "non-solo member" filter (orchestrators, briefings, failover) applies to grouped seats without a second code path. Each grouped seat owns a dedicated group role row (`team_roles.group_id`, `role_key` prefixed with the group's short id, instance numbers continue from the group's max to survive mid-list departures); leaving deletes that row and restores the home role.
- **Lead is decoupled from the role template**: the effective lead of a group is `acting_lead_slot_id ?? lead_slot_id`; authorization overlays `coordination` / `planning` on whoever that is and removes them from a lead-template holder who is not the effective lead. A group may have no lead (decision D2): it then shares goal, messages and memory, the user plans its tasks from the desktop, and `team_groups.plan_policy` decides whether the members may plan too — `any_member` (the default for a group created without a lead) overlays the planning capabilities on every member while no lead exists, `lead_only` (the default with a lead, and the backfill for pre-existing rows) keeps Agents out of `plan`. The policy is inert while a lead exists.
- **Identity is resolved per tool call**: `refreshIdentity` runs before every team tool, returns `groupId`, and an ungrouped seat gets `not_in_group` (replaces `solo_channel`) with `nextAction: enter_channel_wait`. Join / leave therefore takes effect on the Agent's next call — no MCP restart, no token rotation (locked by `tests/channel-mcp-stdio-groups.integration.test.ts` against the real stdio process).
- **Scoping is a write-time snapshot**: `tasks.group_id`, `team_messages.group_id` / `team_message_threads.group_id` (a thread is fixed to the group of its first message) and `team_memory_items.group_id` are additive, column-existence-guarded columns (desktop and MCP processes migrate independently, in any order). Every Agent-facing view filters by the Agent's current group; operator views read the whole run; project-scope memory is shared across groups; legacy team runs (no group on either side) behave exactly as before. Task keys stay unique per run (a cross-group duplicate key is rejected rather than silently doubled); message groups are inferred recipient → sender when the caller does not pass one, so operator / orchestrator notices to a grouped member land in that group.
- **Orchestration iterates per group** (`groupScopedMembers` / `groupScopedLead`): the dispatcher never assigns across groups, a pool run has no executor for a group-less object, run-scoped memory is reviewed inside its group (project-scope by any quality role in the pool), the collaboration sweeper checks each active group's lead heartbeat separately. **The desktop orchestrator is the only scheduler** (phase 2 · 2A, decision D1): it dispatches execution and review, chases stale assignees and re-dispatches after a rejection; the lead is told nothing about that machinery. The lead receives exactly three system notices — task `done`, task `failed` after the last attempt (`TaskDispatcher.notifyOutcome`, idempotent per `taskId:status:attemptCount`), and a group member's confirmed Cursor stop (`TeamCollaborationSweeper.sweepMemberAttention`, once per stop cycle, same evidence as `TeamGroupView.attention`) — and the members' automatic `status` reports. The briefings say the same thing from the Agent's side: the lead plans, answers the user and relays real reports; members do not hand-write status messages. `TeamFailoverService` is only the run-close repair (cancel a completed run's open tasks once); an offline grouped member sets the group's `attention` flag and nothing else. The remedy is the operator's: remove / add, or **membership transfer** (phase 2 · 2C, `TeamGroupService.transferMembership`) — the seat's group identity (group role + lead) moves to another independent seat of the pool in one repository transaction, bindings / tokens / Composers untouched, optionally with the session-context handoff document for the new holder.
- **Membership changes are operator actions** (`TeamGroupService`, IPC `team-group:*`, `RunGroupsPanel` on the 运行 page). The repository transaction (validate, mutate, audit row in `team_group_events`, revision bump) is the truth; a rejected transaction has zero side effects. The side effects run afterwards, each fail-soft: a **membership notice** to the affected seat (`channel_outbox.kind = 'membership'`, silent, delivered by `check_messages` with its own suffix — no `messageId`, no `team_message read`, no `record_reply` — because the stage-0 experiment showed the model treating a mid-session identity change under the internal-notification suffix as an injection); lease / review release plus un-targeting of every non-terminal task aimed at the leaving seat (`releaseAgentWork`, otherwise nobody in the group could ever claim it); pending directives to the leaver marked `orphaned: member left` (undelivered ones flip to `not_required`, the sweeper stops chasing them); a `notice` to the group lead; goal changes as a `notice` to every member; dissolution cancels the group's open tasks (`group_dissolved`) and keeps messages and memory read-only. Inside the pool `team_run start` answers `not_applicable`, and `transfer_lead` / `claim_lead` / `clear_acting_lead` act on the group's acting lead. A grouped seat that is **rebuilt** (`prepareComposerRelaunch`, then a new Composer binds) gets a fresh `joined` notice from `TeamControlService.recordComposerBinding` plus a `member_rejoined_after_rebuild` audit event (actor `system`): the new session started from the solo launch hint and the original notice was consumed by its predecessor.
- **Deferred to later phases** (see `.handoff/DYNAMIC-GROUPS-ROADMAP.md`): adding / removing / re-registering seats without a new batch, per-group usage accounting (2E), the roster-as-groups UI, and the tool-surface reshaping. Team-level checkpoints / one-click recovery are not deferred but retired (phase 2 · 2D, see "Recovery in the pool model").

### The session fence

Channel numbers are global singletons and one MCP process serves every Cursor session, so `channel_id` alone cannot distinguish the old independent session on CH-N from the new team seat on the same channel. The fence makes the arbitration explicit:

- Every `(run, slot)` runtime binding carries a `session_token`, issued at install time and rotated on seat rebuild (`prepareComposerRelaunch`). Re-installing the same topology (top-up, re-activation) does not rotate it (a session that is still polling must not be killed by its own fence). Nothing else touches it: membership transfer moves group identity between seats, never bindings (standby takeover and the binding-moving manual handoff are gone since 2B-2 / 2C).
- Launch hints, role briefings and delivery suffixes hand the token to the Agent and require it on `check_messages` / `record_reply`. Team tools only take `channel_id`.
- On every communication call the MCP server resolves the channel's owner in the active run (`resolveChannelSessionOwner`, one lightweight query) and evaluates the token *before* any presence write: `ok` (token matches the seat), `legacy` (no token presented: pre-upgrade sessions keep the old contract), or `retired` (`no_run`, `run_completed`, `channel_unbound`, `token_mismatch`). A retired `check_messages` returns a plain-text system instruction that is equivalent to the user asking the Agent to stop; a retired `record_reply` returns `code: session_retired`. If ownership cannot be resolved the fence fails open.
- Switching the conversation scope to a different run (`beginScope`) marks every `channel_presence` row `retired`: old heartbeats no longer represent the new seats. `retired` is an explicit stop phase (offline, `runtimeEvidence = stopped`) that only new life evidence revives: the new session's first tool call (`touchPresence`) or CDP runtime activity for a Composer bound in the *current* run (`touchRuntimeActivity`). Replaying the same scope after a restart does not revive it.

Replacing a run therefore no longer hard-blocks on live sessions. `TeamControlService.createSessionPool` is the one entry point (complete the previous run with a "replaced" reason → write the new all-solo bundle → begin the new scope; the fence retires the old sessions on their next poll); `endActiveRun` ends the current pool explicitly. The only hard block is a one-click session creation still running in the desktop. There is no launch step and no "next round": a pool run is `running` from the moment it is written and `completed` when the user ends or replaces it; `team_check_in` on a `completed` run is refused with `run_completed`.

The renderer mirrors the model with a single **运行** page (`src/renderer/src/run/`): `run-view.ts` folds the snapshot into one pool view model (phase `none` / `active` / `completed`, seat states, live-seat count, groups, the consequence text of every destructive action; an archived legacy team run is projected as "no run" plus `archivedLegacyTeam`), `RunHeader` carries the batch summary and the end action, `RunIndependentPanel` the batch configuration, `RunSeats` the seats, and every destructive transition (end, new batch, remove / dissolve a group) goes through one `ReplaceRunSheet` that asks once only while seats are still live. The seat list shows every seat (grouped ones labelled "组 · 角色"), and `RunGroupsPanel` under the batch summary carries the group cards (goal, members, lead select, attention), one shared create / add drawer (ungrouped seats with a role template each, optional lead), and remove / dissolve confirmations that reuse `ReplaceRunSheet` with group wording; preview scene `?independent=groups`. Account and Cursor maintenance live behind the top-right account entry (`account` module), not in the main navigation.

## Desktop restart continuity

Quitting or restarting the 拾光 desktop app does not end any Cursor session, by construction:

- The `SG Team` MCP process is spawned and owned by Cursor (its parent is Cursor's MCP utility process), not by the desktop app. It talks to the desktop app only through the shared SQLite file — no IPC, socket or child-process relationship — so the desktop app's lifetime is irrelevant to an Agent's `check_messages` loop.
- Everything the loop depends on is durable: outbox, replies, presence, the reply-sync gate, run/slot bindings and the per-seat session token. Replies recorded while the desktop is down are consumed on the next `pollReplies`; the timeline hydrates from `channel_scope` on start.
- Startup never rewrites `~/.cursor/mcp.json` unless the entry content changed (`reconcileGlobalChannelServers`), so Cursor is not asked to restart the server. `beginScope` (which retires presence and clears gates) only runs on a real run change, never on a same-run replay.
- The CDP hook re-broadcasts the loaded Composers' process snapshots on attach — both when it installs fresh and when a same-version hook survived a force-quit (`'already'` path) — so the live process card is back before Cursor's next model write. A completed CDP response that matches the latest persisted reply by text identity is treated as already archived, so the final answer is not shown twice after a restart.
- The channel service rides out storage blips caused by the desktop's own start/stop (SQLite lock windows): a failing poll iteration is retried on the poll interval until the keepalive deadline; `record_reply` retries `SQLITE_BUSY/LOCKED` in-process. Only a whole keepalive window of failures surfaces, as a structured `storage_unavailable` result with `retryable` (false after `CHANNEL_STORAGE_RETRY_LIMIT` consecutive failures) — never as a raw "database is locked" tool error.
- The server instructions state the contract to the Agent once ("瞬断续接"): a desktop restart is not a stop; transport / storage blips are retried in place; only the session fence, quota / authorization errors or three consecutive failures end the loop.

What does interrupt sessions is orthogonal: restarting Cursor (the Composers and the MCP process die with it — recovery is the session-handoff / recreate flow), and Cursor reloading its MCP configuration (any in-flight long-poll fails once; the Agent re-enters `check_messages` under the rule above).

## Recovery in the pool model

There is no team-level checkpoint and no one-click recovery (`TeamContinuityService`, `team_checkpoints` / `team_restore_*`, retired in phase 2 · 2D). The pool never ends by accident — the unit of failure is a single Cursor session — and a seat is recovered by parts that exist anyway, each reading live state instead of a snapshot:

- **seat rebuild** (`prepareComposerRelaunch`, then the new Composer binds; `SeatRotationService` does it automatically for solo seats): same channel, rotated session token, group identity kept;
- **session-context handoff** (`SessionHandoffService`): the context document derived from the previous session's transcript, queued as a plain user message into the target channel — on rebuild, or with a membership transfer (`transferMembershipWithContext`);
- **membership notice on rebind** (2C): a rebuilt grouped seat is told which group it is in and to `team_check_in`;
- **`team_check_in`**: the briefing itself — group goal, tasks, unread messages, shared memory — is always the current state of the run's tables.

Everything a recovery capsule used to carry (goal, group members and lead, tasks, pending messages, shared memory, working files) is therefore either live in the run or carried by the handoff document; group structure history stays auditable in `team_group_events`. Raw chat is still never presented as team memory.

## Task pool persistence

The task pool is durable and uses Electron's bundled `node:sqlite`:

- dedicated database under the 拾光 user-data directory;
- WAL mode, foreign keys and busy timeout enabled;
- normalized `tasks`, `attempts`, `task_events` and `task_pool_meta` tables;
- all aggregate saves use `BEGIN IMMEDIATE` plus revision compare-and-swap;
- lease expiry is swept by the Electron main process, independent of the renderer;
- every installed MCP process is bound to an active SQLite agent registration;
- the production MCP bundle is copied to `Contents/Resources/mcp/index.mjs`, outside `app.asar`, and executed through the packaged Electron binary with `ELECTRON_RUN_AS_NODE=1`.

## Cursor account switching

Switching accounts (FlyCursor-style one-click flow) is orchestrated in the main process: deterministically terminate Cursor (macOS `pkill -x` → poll → `pkill -9` fallback; Windows `taskkill /F`) → logical key backup → write auth keys + account-bound machine identity into `state.vscdb` / `storage.json` / `machineid` exclusively (Cursor must be dead: `node:sqlite` is synchronous and touching the DB while Cursor runs froze the main process) → relaunch with `--remote-debugging-port`. On Windows the relaunch uses the executable path captured from the running process before it was terminated (per-user and all-users Inno Setup installs live in different directories and the latter registers no App Paths entry).

## Platform boundary

Both desktop platforms are first-class. Everything that touches Cursor's own files goes through `src/infrastructure/cursor/cursor-install-paths.ts` (user-data root, install roots, workbench bundle), and process control goes through the platform pair `open/pkill/pgrep/osascript` (macOS) vs `cmd start/taskkill/tasklist/PowerShell Get-Process` (Windows). Two account-automation browser hosts exist: the fingerprint browser (RoxyBrowser, both platforms) and the external system browser (macOS only, AppleScript); Windows always uses the fingerprint host.

## Distribution boundary

`npm run pack:mac` creates an unsigned local macOS app and `npm run pack:win` an NSIS installer plus `win-unpacked` under `release/`. `npm run verify:mac` / `verify:win` launch the MCP bundle through that packaged executable and repeat the real three-process fencing/resume smoke. Signing and notarization are intentionally a later release step and are not implied by the local package.

### Cursor process delivery hydration (2026-09-07)

- Hook v25 and runtime inspection decode Cursor's protobuf `case/value` MCP results, including modern content blocks. Only positive user-delivery matches are memoized: a completed tool may receive its result in a later hydration frame. Inspection ignores legacy cached negative matches.
- Queue delivery and its reply-sync presence update commit in one SQLite transaction. A failed presence write rolls back delivery; retry advances the delivered count only after commit.
- Virtual turn boundaries remain based on actual outbound delivery and reply timestamps. No guessed offset is applied to native bubble timestamps. Restored Cursor history may carry hydration-time timestamps; this change does not reconstruct missing historical timing.
- Verification: full typecheck, 139 test files / 1270 tests, knip, build and stdio channel smoke passed. Read-only evaluation against the existing Cursor Composer confirmed modern delivery recognition and visible following thoughts; no new Agent turn was sent for live animation acceptance.

### In-app Cursor questions and process presentation (2026-09-09)

- Hook v26 projects Cursor tool intent, action, target and result hints separately, and maps native `ask_question` bubbles from `additionalData.status` instead of treating the completed tool shell as an answered question.
- Answering stays on Cursor's native runtime path: 拾光 writes `currentSelections` / `freeformTexts` through ToolFormer, then submits a non-empty decision note through the existing Composer bridge. Every write is scoped by `composerId + toolCallId`, verifies that the decision is still pending, and degrades to answering in Cursor when the runtime surface is unavailable; no Cursor bundle patch or MCP restart is involved.
- A pending user decision remains a live execution state. CDP refreshes runtime activity while Cursor is blocked, and desktop/run projections surface the seat as `等待回答` so failover and session rebuilding do not mistake it for an offline Agent.

- 2026-09-09 closeout: responder requires the matching blocking pending decision and an explicit submitted/cancelled acknowledgement; the application rejects acknowledgement/action mismatches. Executable VM tests exercise the generated write expressions, not only string matching.
- Verification: typecheck, 148 test files / 1351 tests, knip and build passed. Browser preview verified multi-selection, submit and expanded answer rendering. Real Cursor answer/skip continuation remains unverified: the local CDP endpoint was not reachable during closeout. No real questionnaire was answered, no Cursor/MCP process restarted.

### Cursor live account switch (2026-09-10)

- A live switch keeps the Cursor process and Composer sessions alive: under one shared hot/cold mutex, 拾光 verifies the installed pump, exchanges the selected vault credential for desktop tokens, serves one nonce-bound payload on the pump's exact loopback endpoint, and accepts success only after Cursor posts a hard readback acknowledgement.
- The acknowledgement is followed by one atomic vault transition (`activateAfterLiveSwitch`): target account becomes active and receives the machine-alignment marker in the same file replacement. A successful cold switch uses `activateAfterColdSwitch` to select the target and clear all markers atomically.
- Compatible `ZMO_SWITCH_V1` pumps installed by another tool are capability-checked and may be reused. They carry no 拾光 owner marker, so the installer does not overwrite or uninstall them. Managed pumps include an owner marker and template revision; marked-but-incomplete runtimes are reported as unsupported.
- Automation starts the live switch after processing and runs it beside the hardening/delete path. Every terminal branch consumes the run-bound result; a failed first attempt retries the same selected successor, never the processed account. Cursor identity hydration receives a 15-second observer grace window.
- A live switch does not change the machine identity. The account row exposes `pendingMachineAlign`; a later cold switch aligns it. Partial success (Cursor switched but local vault commit failed) is reported as a warning, not as total failure.

### Per-account fingerprint window binding (2026-09-10)

- The automation pipeline's fingerprint window is no longer a single global selection: accounts carry a persistent `fingerprintProfileId` binding (recorded automatically at import time — whichever window the token was read from), and the global `bitProfileId` setting degrades to the "default window" for new imports and unbound accounts. `resolveExecutionProfileId` (domain) is the single resolution rule — active account binding ?? default — shared by main-process wiring and the settings-page preview text so the UI can never describe a different window than the one used.
- Window source semantics are split by intent: user-explicit operations (import / open-login-page / manual cleanup) pass their target window explicitly, while mid-run automation calls pass none and reuse the live channel session as the run anchor. This makes a mid-run live handover safe: the active account (and thus resolution) changes, but the deletion chain stays on the window where the run started.
- Ownership guards against wrong-account deletion: the in-browser delete path verifies the page session's account id prefix against the processed account before firing, and the legacy cookie-rotation path refuses to persist/delete with a rotated token whose account prefix differs. Guards engage only for `user::jwt`-shaped tokens and degrade to the legacy behavior otherwise.
- Settings UI: accounts render as a two-column card grid (header identity / meta chips / actions) instead of full-width rows; each card exposes a compact binding chip (window name or 默认窗口) with in-place rebinding; the automation group footer reports the actually-resolved execution window for the current active account. Rebinding does not touch `updatedAt` (handover preference ordering is an account-activity signal, not a UI concern).
- Verification: full typecheck, 152 test files / 1424 tests, knip and build passed. Browser preview verified bound/unbound row states, dropdown rebinding, the automation follow text, and the account card grid in light/dark × wide/narrow; real Roxy end-to-end run remains unverified.

### Anti-loop-proof polling: the tick cursor (2026-09-12)

- Incident: a seat agent stopped its `check_messages` long-poll after consecutive keepalives and announced a "polling pause" to the user, breaking the long session. Trigger: the host IDE's anti-loop safeguard injects an authoritative "repeating the same tool calls … ask the user for guidance" reminder, and the long-poll pattern (identical `check_messages` arguments every ~60s) trips it by design. The compacted delivery suffix (single mild "静默续等" clause) no longer outweighed that reminder.
- Cure, mechanism first: every non-error `check_messages` result now names the exact next call, including a fresh `tick` — the persisted per-channel turn counter, monotonic across deliveries, restarts and MCP process respawns. `check_messages` accepts an optional `tick` string the server never validates (fail-open); its only job is to make every poll's arguments unique so identical-call detection never fires. The keepalive body keeps the `<sg_team_keepalive n="N"/>` marker untouched (observer/test contract) and stays a single minimal line — marker + exact next call + a six-character false-positive note — because idle keepalives accumulate in the model context once per minute and their cost must approach zero (the same minimalism constraint that motivated the suffix compaction: no thinking, no prose, straight into the long-poll state).
- Cure, text second: server instructions state the tick contract and name the IDE reminder a false positive; the delivery suffix (user-visible and silent alike) again carries the strong keepalive prohibitions ("直接再调、不输出可见回复、不以任何文字代替调用") that the earlier compaction had dropped; the solo launch hint now explicitly says "不思考、不输出任何文字" before the first `check_messages`.

### Live-response lifecycle guard (2026-09-12, flicker fix)

- Bug: the "Cursor 实时生成中" badge flickered at frame rate while the process stream was presenting. `updateLiveAgentResponse` treated every runtime-inspect frame's `isGenerating` as lifecycle truth; during tool execution the bridge summary reads not-generating (no tokens produced) while observer native frames read generating, so 150ms inspect frames flipped the entry streaming→complete and observer frames flipped it back.
- Fix: the live-response path now follows the exact rule the process view got earlier (`inspectMayEndTurn`) — entries carry a `source` (`native` / `inspect` / `transcript`); while the observer is connected and the entry is observer-owned, inspect frames never end or resurrect it (blocked frames only refresh `updatedAt` as keepalive, since observer frames carry no response text during tool calls); with the observer offline, inspect may end a streaming entry only after 2s of quiet. Ownership upgrades to `native` on authoritative frames and never downgrades on inspect refreshes.
- Verification: typecheck, 156 test files / 1500 tests and knip passed; new guard tests cover the blocked flip, the keepalive refresh, the authoritative completion, the blocked resurrection, and the degraded-mode 2s rule.

### Cursor renderer lag after ~1h: hook scan cost cure (2026-09-12)

- Root cause: the in-page stream hook wrapped the composer data model's write methods and ran a full `processSnapshot` over every bubble of the current turn on every model write (several per second while streaming). Per-bubble derivations (toolInfo JSON walks, thinking concatenation, safePlain clipping, diff parsing) plus a up-to-3MB `JSON.stringify` + CDP binding call per frame made the cost grow O(turn²) — after an hour a turn holds thousands of bubbles and every token write stole Cursor's renderer main thread.
- Cure (hook v30), semantics-preserving:
  - Process payloads are throttled to a 100ms trailing window: frames inside the window carry only the small response/state payload (typewriter granularity untouched), the trailing flush and every terminal (`isGenerating=false`) frame always carry the full process set. The desktop already treats a missing `process` as "no process this frame, keep the previous view", so no desktop change was needed. Escape hatch: `__sgTeamProcessThrottleMs` (0 = every frame full).
  - Per-bubble derivation cache: `toolInfo`/`present`/`safePlain`/`thinkingInfo` results are cached per `bubbleId` with a cheap version fingerprint (lengths / statuses / hydration markers, incl. a deep result fingerprint for late-hydrating MCP results) plus a reference-identity fast path (immutable updates recompute, in-place mutations are caught by the fingerprint). Generating bubbles and terminal frames always recompute fresh, so archived/persisted process blocks never come from cache. Escape hatch: `__sgTeamFactCache === false`.
  - `isUserDelivery` negative caching was deliberately NOT done: a completed tool's result can hydrate in a later frame (2026-09-07 incident), and a wrongly cached negative would permanently hide a real delivery; keepalive results are tiny, so the re-parse cost is negligible.
- Equivalence guard: a test replays a growing turn (thinking streaming, MCP name hydration, result hydration, streaming edit diff, response streaming, terminal frame) through cache-on and cache-off hooks and asserts byte-identical frames; a second test locks the throttle's leading/trailing/terminal frame shapes.
- Verification: typecheck, 156 test files / 1515 tests, knip passed.
- Verification: typecheck passed; channel protocol policy and MCP integration tests updated and extended (tick echo in keepalive with/without session token, tick passthrough, suffix tick hints).

### Post-reply continuation (2026-09-12, handoff process-stream fix)

- Incident: after a session handoff the receiving seat replied "已接手…" via `record_reply` and then kept working on the inherited task inside the same Cursor native turn instead of returning to `check_messages` (09-10: 32 business tool calls, zero polls; 09-11: 243 calls over 1.5 h). The virtual-turn model classified every block after the reply's close boundary as transport gap — dropped by the renderer, skipped by the seal — so the target session showed no live process at all and the work never reached history. Queued user messages could not be delivered either, because the agent never polled.
- Model change: `partitionVirtualProcessBlocks` no longer emits a `gap`; blocks between `reply.createdAt` and the next delivered message form the anchor's **continuation** segment. This is sound because transport noise (check_messages / keepalive thinking / capability) is already filtered per bubble in the hook's `processSnapshot`; what reaches the service after the seal is business work by construction. Legacy replies without `replyToEntryId` keep the time-window fallback and never get a continuation.
- Persistence: `channel_replies.continuation_blocks_json` (idempotent column migration). The seal persists only settled (`done` / `failed`) continuation blocks — appended, immutable once written, clipped by `clipProcessBlockForPersistence` — while running ones stay live until they settle; `process_blocks_json` therefore keeps its byte-stable contract. Persisted ids join `committedProcessBlockIds`, so window replays and restarts do not re-add them to the live view.
- Renderer: `projectVirtualProcessTurns` yields `continuation` (process + response routed by the same close boundary); `projectTurnTimeline` merges `reply.continuationBlocks` with the live tail under the same `turn:<outboundId>` identity; `SessionWorkspace` renders it as a grouped Agent row directly under the reply with the caption "回复后继续工作(中)". Inspector activity / review scope / todo panels and the handoff record read `conversationEntryProcessBlocks(entry)` (reply process + continuation).
- Handoff text: the closing instruction now tells the recipient to return to `check_messages` right after confirming and not to resume the transcript's unfinished task on its own (`HANDOFF_CLOSING_INSTRUCTION`).
- Known limit, unchanged: a `record_reply` issued at the end of a continuation (no newly delivered user message) is still classified as a background reply (`visible = 0`); its text shows live through the continuation response but is not persisted as a timeline entry.
- Verification: typecheck, knip, build and `smoke:channel` passed; event-level tests cover the domain partition, timeline merge, service seal sequence (running → settled → append → window replay byte-stable → next turn isolation → restart hydration), repository/relay persistence, workspace rows and the handoff wording; preview scene `?continuation=1` with `session-continuation-{light,dark}` screenshots.

### Turn liveness vs token streaming: the session-rail activity flicker (2026-09-12)

- Symptom: the "what is it doing" bar under a session-rail card (`.session-row__activity`) blinked on and off while an agent worked, and only ever said `思考中` — never the tool being run.
- Root cause (verified with a read-only CDP probe against live Cursor 3.6.31, not inferred): `ComposerData` has **no** `isGenerating` field; it carries `status: 'generating' | 'aborted' | …` (turn lifecycle) and `generatingBubbleIds` (bubbles currently emitting tokens). The hook derived the frame-level `isGenerating` from `data.isGenerating === true || generatingBubbleIds.length > 0`, which degenerates to the bubble-level set — and that set is **empty during every tool execution** (`tool=run_terminal_command td=loading gCount=0`). So every tool call produced a "terminal" frame: `mergeProcessEvidence` forced running tools to `done`, set `view.generating=false`, archived the turn, and the next thinking token re-opened it. The rail projection is gated on `liveProcess.generating`, so the bar was visible only while a thinking/text bubble streamed — the inverse of what it is for. The same misclassification stamped `completedAt` on executing tools and hid the shell card's live-output state on the session page.
- Fix, one rule per layer:
  - Hook v31 (`cursor-stream-observer.ts`): frame `isGenerating` = turn liveness = `status === 'generating' || isGenerating === true || generatingBubbleCount > 0` (bubble streaming implies liveness; `status` adds the tool-execution stretch the bubble set cannot see; legacy shapes without `status` fall back to the v30 behavior). Bubble-level streaming stays in `process.generatingBubbleCount` and `response.generating`; `parseNativeResponse` now keeps the latter instead of dropping it.
  - Service (`desktop-session-service.ts`): the live answer's `streaming → complete` follows `response.generating` (the frame-level flag stays true while the agent goes on to `record_reply` in a persistent session; using it would leave the finished answer streaming until the 2.5s stale sweep). The live state records `streaming` separately from `generating`; the 150ms runtime fast loop is gated on `streaming`, so long tool runs do not pay a full-turn inspect every 150ms.
  - Rail projection (`session-rail-view.ts`): `sessionRailActivity` is an ordered rule list — offline → none; awaiting user → `等待回答`; turn dead and no streaming answer → none; newest running block → its verb + target; streaming answer → `回复中`; block gap → hold the newest settled block; nothing visible → none. The `处理中` placeholder for "alive but empty view" is gone: it flashed after every reply seal and lied about what was happening. Enter animation is a 120ms fade (no translate). *(Superseded the same day by « Session-rail status line » below: the "none" outcomes and the fade were themselves the remaining blink; the line is now Cursor's persistent subtitle.)*
- Verification: hook VM tests for the three `status` shapes plus `response.generating`; service event sequence thinking → tool(running, 0 streaming bubbles) → gap → thinking → real end asserts `generating` stays true, the tool stays `running`, no archive until the real end, and the rail reads `读取中 · src/main/index.ts` throughout; live-answer completion by bubble flag; rail projection sequences. Preview `sessions-rail-activity-{light,dark,narrow}` re-shot. A real-session check (agent running a >10s command while watching the rail) is still pending — the running desktop build predates this change.

### Queued messages leave the timeline: the delivery tray (2026-09-12)

- Model, stated once: a user message enters the dialogue when `check_messages` takes it (`deliveredAt`), not when it is sent. `sortConversationEntries` already ranked undelivered messages at `+∞` for that reason; the renderer now follows the same rule. `partitionTimelineEntries` (`timeline-view.ts`) splits the visible entries into `timeline` (delivered content) and `queued` (undelivered desktop user messages, `isQueuedUserEntry`) — mutually exclusive and exhaustive, so a message is shown in exactly one place. Immediate-delivery transports have no queued state.
- Presentation: undelivered messages dock in `QueuedMessageTray` between the timeline and the composer (a fifth `workspace-main` grid row, dashed frame, same width as the composer). The tray is the optimistic echo of "send": header = server-side pending count + presence state line (listening / processing / offline) + held-for-next-session badge; rows = time, attachment count, hold marker, preview, `放行` / `撤回`. It collapses to its header, renders a one-line note when only internal silent messages are pending, and is absent when nothing is pending. The composer's queue chip + hover popover were removed rather than kept as a second rendering of the same list. On delivery the entry moves from the tray into the timeline under the same `turn:<outboundId>` identity; `useBottomFollow` now also observes the viewport, so the shrink/grow caused by the tray (or the composer) keeps a following timeline pinned to the bottom.
- Consequence for run end: `retireScopeBefore` / `beginScope` retire undelivered rows, and a retired message is not "pending" any more. `ChannelOutboundMessage.retiredAt` is now mapped; `entryFromOutbound` skips retired-undelivered rows on hydration and `refreshOutboundDeliveries` (also invoked by `completeScope`) drops them from the in-memory timeline in the same scan that syncs `deliveredAt`. Previously such rows lingered as a user bubble labelled 「排队中」 forever; now they leave the session page, matching the run-end confirmation text ("尚未取走的排队消息将归档").
- Deliberately not done: a Cursor-style "send now" (delivery is pull-based; forcing it means interrupting the Cursor turn), and any special casing on `session.waiting` (the pending→delivered flip is observed from SQLite only, one rule, no intermediate-state flicker).
- Verification: typecheck, 157 test files / 1546 tests, knip and build passed. New tests: tray SSR + interaction (`queued-message-tray.test.tsx`), partition (`timeline-view.test.ts`), workspace placement / delivery transition / held messages / immediate transport (`session-workspace.test.tsx`), relay retirement + restart hydration (`channel-message-relay.test.ts`), CSS contract (`theme-surface-contract.test.ts`). Preview scene `?queued=1` with `session-queue-tray-{light,dark,collapsed}` screenshots and a geometry probe (tray below the timeline, above and flush with the composer, queued text absent from every bubble); a live browser walkthrough on the waiting seat showed tray → timeline within ~1s with the viewport staying at the bottom.

### Cursor lag after ~1h, second root cause: the legacy bridge's trace wrapper (2026-09-12)

- The hook-side cure (v30 throttle + fact cache) left the hook at ~5% of Cursor's renderer main thread; a 15.7s sampling CPU profile of the live workbench (read-only CDP `Profiler`) still showed the main thread 81% busy with ~46% in one chain: `JSON.stringify` **with a replacer** at `workbench.desktop.main.js:59118` (3.7s) and Cursor's own `giantJsonStringify` guard describing the object shape (`eLf`, 3.5s).
- That replacer belongs to the **QingTian-era bundle patch** (`__QINGTIAN_SEAMLESS__` / `__QINGTIAN_COMPOSER_BRIDGE_V2__`, runtime `20260624-native-stream-v4`, 5,025 lines appended to the local `workbench.desktop.main.js` by an earlier tool, not by this repo). It wraps every `window.__qtComposerBridge` method in `_qtTrace` and serializes the **entire return value** through `_qtTraceSafeJson` (replacer + linear `seen.indexOf` → superlinear) to keep a 900-character preview. `bridge.getComposerData` therefore costs 34–97 ms per call at 1–9 MB of composer data (measured: raw `composerDataService.getComposerDataIfLoaded` is 0 ms for the same object), and every > 1 MB stringify additionally trips Cursor's giant-stringify Sentry guard.
- 拾光's runtime inspect (`buildRuntimeInspectionExpression`, 120–150 ms cadence, one call per bound Composer) was the hot caller: ~266 ms of renderer main thread per round for four seats after 45 minutes, growing linearly with composer size (the persistent `check_messages` turn never ends; the most active seat grew 9 → 18 MB in 20 minutes; two image deliveries alone account for 2.8 MB because the MCP result is stored twice per bubble).
- Fix: the inspect reads composer data straight from `window.__qtComposerService.composerDataService.getComposerDataIfLoaded` (same source the bridge delegates to) and only falls back to `bridge.getComposerData` when the native service is absent. `getStatus` / `listComposers` stay on the bridge (1–3 ms). Test pins that the wrapped method is never called when the native service exists.
- Still open, recorded here rather than fixed: (a) the app depends on the legacy patch's `__qtComposerBridge` for session creation, `getStatus`, `listComposers` and question submission — owning a minimal CDP-injected bridge would remove the last plugin-era dependency and its 500 ms poller / dead-port `fetch` to `127.0.0.1:36530`; (b) the desktop telemetry `json_extract`s whole `composerData:*` blobs from a 22 GB `state.vscdb` on every changed tick; (c) composer size itself is the multiplier for every remaining per-write cost inside Cursor — bounded only by rotating the seat to a fresh Composer (existing handoff flow) once it grows past a threshold.

### Cursor local storage cleanup: catalog-driven, trash-first, DB pruning in a worker (2026-09-12)

- Why: the machine the lag investigation ran on carried 22.4 GB of `state.vscdb` (1.09 M bubbles / 1,922 composers, `freelist = 0`), 16 GB of checkpoint `snapshots/`, 2.2 GB of `workspaceStorage/` with ≥ 20 orphans and 1.7 GB of stale `state.vscdb.backup/.bak`. None of that is the 1h lag (that is renderer CPU, see the two entries above), but it is 40 GB of disk and makes every write and every telemetry read slower. Settings gained a sixth group, **存储清理** (`SettingsCleanup`).
- Shape: a domain **catalog** (`cursor-storage-cleanup.ts`) describes eight items in user language — what it is, what is lost, risk (`none/low/medium/high`), whether Cursor must be closed, whether it is irreversible or diagnostic-only. Everything downstream (scan entries, default selection, the plan, the confirm copy) derives from the catalog; there is no second list anywhere. `buildCleanupPlan` folds "what the user ticked" into "what can run now": diagnostic items, empty items and unknown ids are blocked with a reason; items that need Cursor closed are blocked while Cursor runs **or while the process state is unknown** (never move files on an uncertain state). The renderer and the main process run the same function — the main process re-scans and rebuilds the plan right before executing, so a stale panel can never delete on old numbers.
- Execution: directory items go through `shell.trashItem` (recoverable; `snapshots/`, `History/`, `logs/` keep the directory and trash its children; cache dirs and orphan workspace dirs are trashed whole); one failing path is recorded in `skipped` and the other items continue. Chat history is the only irreversible item: candidates are indexed composers whose `lastUpdatedAt` is older than the chosen 30/90/180-day threshold, minus every Composer bound to any 拾光 run (`runtime_bindings`, all runs) and minus composers flagged `isBestOfNSubcomposer / isProject / isSpec / isWorktree` (referenced by a parent, by Projects / Specs, or by a worktree on disk — deleting them leaves dangling references). Undated headers are kept. The panel shows protected and skipped counts so the number "298 of 1321" is explainable.
- The database work runs in a `?nodeWorker` thread (`cursor-storage-worker.ts`, one worker per request, first message is the result, then terminate): on a 22 GB WAL database a `bubbleId:` index range count is ~6 s cold and a `length(value)` sum over 800 K bubbles took 190 s — neither may touch the Electron main thread. Size is therefore **estimated**: candidate `composerData` bytes + (sampled mean bubble size × candidate bubble count); the UI labels it 约. Pruning deletes the eight per-composer key families (`composerData`, `bubbleId`, `checkpointId`, `ofsContent`, `messageRequestContext`, `codeBlockDiff`, `codeBlockPartialInlineDiffFates`, `composerVirtualRowHeights`) **one composer per transaction**: a single transaction over a million rows would grow the WAL by the size of the deletion on a disk with 21 GB free and roll everything back on failure; per-composer commits let auto-checkpoint recycle the WAL, a mid-run failure keeps what was deleted, removes only those ids from `composer.composerHeaders` and the per-workspace `composer.composerData` indexes, then throws with progress ("已删除 N / M"). `wal_checkpoint(TRUNCATE)` runs at the end; `VACUUM` only when the user opts in and free disk ≥ 1.1 × file size (otherwise the freed pages are reused by Cursor's later writes and the panel says so). `agentKv:blob:*` (content-addressed) and `inlineDiff:<workspaceHash>:*` are never touched.
- "Is Cursor running" has one outlet now: `cursor-process-probe.ts` (`pgrep -x Cursor` / `tasklist`, exit code 1 = not running, any other failure throws). The account switcher delegates to it (same fail-closed contract, same error text); the cleanup uses the tri-state wrapper (`undefined` blocks anything that needs Cursor closed). The CDP restart keeps its own "any Cursor process still alive" count for quit polling — a different question, deliberately not merged.
- Not done, recorded: the worker chunk ships inside `app.asar`; Electron's worker_threads + asar support has edges, so the first packaged run must verify the worker starts (fallback: `asarUnpack` for `out/main/cursor-storage-worker-*.js`). `legacyPatch()` reads the whole 20 MB bundle to find the marker (a tail read would do). No progress reporting during a multi-minute VACUUM (the port protocol is one message per worker).
- Verification: domain matrix (`cursor-storage-cleanup.test.ts`), analysis + prune on a seeded `state.vscdb` including the partial-failure path via a `RAISE(ABORT)` trigger (`cursor-state-db-analysis.test.ts`), scanner on a temp user-data tree (`cursor-storage-scanner.test.ts`), probe tri-state, worker port timeouts/exit codes, IPC input validation, panel behaviour incl. the `#account:cleanup` deep link (`settings-cleanup.test.tsx`). Preview scenes `settings-cleanup-{light,dark}`, `-closed-light` (tall, all eight rows), `-confirm-dark` (irreversible confirm block), `-empty-dark`.

### Inspector tab pills (2026-09-12)

- The right-hand inspector tabs are icon pills: the four tabs show only their icon (32×32); the selected one grows its pill in place and the label + count slide out to the right of the icon (`grid-template-columns: 0fr → 1fr`, 220ms), collapsing again when another tab is chosen — the growing and shrinking pills animate together so the bar width barely moves. Selection is owned by the button itself; the separately measured, ResizeObserver-driven sliding indicator is gone, which removes the misalignment it produced while widths changed. The live status dot sits on the icon's top-right corner in both states; the count stays in the tooltip while collapsed. Label and badge remain in the DOM (accessible name and tests unchanged); the ≤360px container query no longer needs to hide labels. The bar is `6 / 32 / 5 + 1px border` so pills and the close button sit exactly centered.
- Verification: `inspector-tabs-{light,dark}` / `-switch-mid` / `-switch-end` / `-narrow` screenshot scenes with a geometry probe (collapsed tabs 32×32 with zero reveal width, active reveal > 0, close button centered with the pills); frame sampling during a switch showed the new pill growing 32 → 93px while the old one shrank 87 → 32px in lockstep with no text overflow.

### Windows readiness: CI trap, process probes, reserved ports (2026-09-12)

- CI had been red on both runners since 09-06 at `lint:dead`, hiding the Windows `npm test` regression that started two runs earlier. Root cause: `summarizeBrowserToken` was exported from `cursor-browser-token-reader.ts` and consumed only by `scripts/manual-account-automation-run.ts`, which is gitignored — knip passed locally (the entry glob `scripts/*.{ts,mjs}` sees the ignored file) and failed on a clean checkout. The helper now lives inside the local script; rule of thumb: nothing under `src/` may be kept alive solely by a gitignored script.
- Every Windows PowerShell probe goes through `windowsPowerShellCommandArgs`, which prefixes `[Console]::OutputEncoding=New-Object System.Text.UTF8Encoding($false)`. Windows PowerShell 5.1 writes pipes in the OEM code page (GBK on zh-CN), so a Chinese user name or install directory came back as mojibake and the relaunch (`cmd start "" "<path>"`) failed.
- The CDP keeper's Windows probe selects the Cursor **main** process (the `Cursor.exe` owning the main window, else the earliest-started one) instead of `Select-Object -First 1` over all same-named renderer/GPU/utility processes, whose churn changed the fingerprint and re-armed the auto-heal countdown.
- The hot-switch loopback bridge treats `EACCES` like `EADDRINUSE`: Windows reserves dynamic port ranges for Hyper-V / WSL2 / Docker (`netsh int ipv4 show excludedportrange protocol=tcp`), and `bind` on a reserved port reports `EACCES`. The cold-switch range scan skips such ports; the exact-port hot switch explains that the pump port is baked into the bundle and must be reinstalled on another port. The pump installer explains `EPERM/EBUSY/EACCES` writes on Windows (Cursor still running, antivirus scan, all-users install under Program Files).
- `workspace-review-reader.test.ts` compares against `realpathSync.native` because the reader uses `fs.promises.realpath` (libuv): on the Windows runner `tmpdir()` is an 8.3 short path (`RUNNER~1`) that only the native call expands — the JS `realpathSync` expectation could never match there.
- Still open, and the real Windows blocker: session creation, runtime `getStatus` / `listComposers`, the process-stream hook install, question answering and warmup all require `window.__qtComposerService` / `window.__qtComposerBridge`, which only the QingTian-era gateway patch provides (5,025 lines appended to the local mac bundle by the retired plugin; this repo has never shipped an installer for it). A clean Cursor 3.6.31 on Windows therefore fails one-click session creation with `bridge_not_ready` while channel communication, team tools and account switching keep working. Options recorded in the CH-3 analysis: port the legacy injector, package the extracted patch as a managed patch next to the switch pump, or build the minimal CDP-injected bridge the previous entry already proposed.
- Verification: typecheck, 157 test files / 1552 tests, knip. Windows behavior is covered by platform-injected unit tests only; no Windows machine was available in this round.

### Session-rail status line: Cursor's own subtitle, persistent (2026-09-12)

- Symptom: the rail's activity bar was intermittent **by design**. Its rule list returned nothing when no business block was visible, and the service produces exactly that state after every reply seal (blocks withdrawn, `generating` still true) and during every `check_messages` long-poll (transport noise is filtered in the hook, so `items` is empty and `liveCursorProcess` is deleted). A seat therefore went blank between "reply sealed" and "next thinking token", and the 120ms mount fade turned every gap into a visible blink. Cursor's own agent list never goes blank: its row subtitle is a status line that always says something.
- Source of truth, read out of `workbench.desktop.main.js` (3.6.31) with `rg`, not inferred: `Tu_` (current action label, wired by `D4i`). Pending human review first (`Awaiting plan review` / `Awaiting edit approval` / `Awaiting terminal approval`, with `Awaiting approval to edit|run …` when params allow). While generating, scan `fullConversationHeadersOnly` newest → oldest: `bubble.thinking !== undefined` → `Thinking` (field presence, content ignored); a tool bubble → `n/m To-Dos Completed` for todo tools when the composer has todos, else `s31(tool, params, status, {verbForm:'loading', includeVerbWithoutDetails:false})` = loading verb (`oit`, overridable by `eKC`) + detail (`i31`) — `Reading foo.ts L1-40`, `Grepping x in dir (glob) [type:ts]`, `Listing current directory`, `Searching files **/*.ts`, `Reading lints in App.tsx`, `Updating todos to-do list` — and tools without a detail (shell, MCP, task, await, ask_question, fetch, get_mcp_tools, plan, …) are **skipped**; an AI text bubble → first line (`o31`), Markdown stripped (`l31`), 50 chars + `…` (`a31`); nothing found → the pending-decision wording (`Awaiting approval`, `Awaiting approval to run <mcp tool>`, …) or `Planning next moves`. Not generating → `Completed` / `Stopped`. `eKC` swaps the verb by path: `~/.cursor/projects/<ws>/terminals/<id>.txt` → `Reading terminal <id>`, `agent-tools/<x>.txt` → `Reading tool output`, anything under `agent-transcripts/` → `Reading agent transcript …`, `<skill root>/…/SKILL.md` → `Using <skill> SKILL.md`, the `terminals` / `agent-transcripts` directories themselves → `Listing terminals` / `Listing agent transcripts`. This is exactly why a standby seat reads `Thinking` in Cursor: `check_messages` is an MCP call with no detail, so the scan walks past it (and past `record_reply`) to the keepalive thought that precedes each poll; a seat that went text → `record_reply` → `check_messages` without thinking reads its own first sentence instead.
- One implementation, three callers: `domain/cursor-status-line.ts#cursorStatusLineOf` is self-contained (no closure, no import, plain ES2022) and is injected into the page by `.toString()` — the `nativeUsagePayload` trick — by hook v32 (`processSnapshot`: every frame incl. throttled small frames, only while the turn is alive; amortised O(1) tail scan, measured ≤0.4 ms on a 117-bubble composer) and by the runtime-inspect fallback (`buildRuntimeInspectionExpression`, the only source while the observer is offline). `cursorStatusLineFromBlocks` runs the same function over 拾光 process blocks as the renderer's last resort; those blocks are transport-filtered, so on that path a standby seat reads `Planning next moves`, not `Thinking` — the fallback's inherent floor, not a rule divergence. The one deliberate deviation from Cursor's text: for the `terminals` / `agent-transcripts` directories Cursor concatenates verb and object into `Listing terminals terminals`; we keep the verb only.
- Service: `liveStatusLines` is its own in-memory map (`LiveStatusLineState`: `composerId`, `generating`, `composerStatus`, `statusLine`, `updatedAt`) fed by observer frames and inspect rows through one `updateLiveStatusLine`. Frames that carry neither `composerStatus` nor `statusLine` (hook ≤ v31) never touch it; an out-of-order guard keyed on the last **accepted** `observedAt` (not the state's `updatedAt`, so unchanged facts keep their object for the renderer memo while time still advances) stops a late inspect row from regressing a newer hook frame; `statusLine` is dropped when `generating` is false. It never enters `liveCursorProcess`, the seal or SQLite — `getSnapshot()` stays zero-write and `process_blocks_json` byte-stable — and it is excluded from the runtime-evidence fingerprint (it has its own change test). The snapshot projection omits entries whose channel is now bound to a different Composer (seat rebuild).
- Rail projection (`sessionRailActivity`, supersedes the rule list in « Turn liveness vs token streaming » above) always returns one line, first hit wins: offline → `Stopped` (explicit termination or Cursor `aborted`) / `Completed`, muted, no spinner; `starting` → `Starting up`; `awaitingUser` (authoritative, else a pending question block) → `Awaiting approval · <question title>` — Cursor's wording for the pending-decision group; 拾光 adds the ask_question title, the one place the pill is richer than Cursor's row; Cursor facts present → the subtitle verbatim, tool labels split back into verb + object for the pill (`Reading` · `foo.ts`), `Planning next moves` when Cursor scanned nothing, `Completed` / `Stopped` when the turn ended; no facts → the same algorithm over process blocks / streaming answer, `Completed` for an ended turn, `Planning next moves` when nothing is known but the seat is on duty. The pill is persistent: no mount animation, Cursor-style spinner while the turn is alive, static tool glyph otherwise, muted when settled; wording is Cursor's English verbatim (`CURSOR_STATUS_LINE_TEXT`). Geometry: the pill is the card's bottom row and spans both grid columns (avatar left edge → card right edge), so every row's pill is the same width and the spinner sits at its left end; long objects and long text snippets ellipsise inside it. An intermediate version kept the pill inside the text column at content width; it left the area under the avatar empty and the right edges ragged, and was dropped the same day.
- Preview: `?railactivity=1|long` (with `sessions=many`) puts every form on one roster — `Grepping`, `Reading` (block fallback, long name in `long`), `Editing`, text snippet, `Awaiting approval`, `To-Dos`, standby `Thinking`, `Planning next moves` (a ninth seat that exists only in this scene) and offline `Completed`. The narrow probe asserts one pill per row incl. offline, the pill spanning avatar-left → card-right on every row, long details and long snippets ellipsising, spinner ⇔ live, no enter animation, the standby seat reading `Thinking`.
- Verification: read-only CDP probe on the four live seats with the compiled `cursorStatusLineOf` injected verbatim — CH-1 `Editing probe-domain.mjs` (running shell skipped), CH-2 `Thinking`, CH-3 text snippet on an `aborted` composer, CH-4 its own first sentence after `record_reply` — and the bubble shape confirmed (`toolFormerData` carries the `tool` enum, `name`, legacy `params`, and modern `toolCall.tool.case` + `value.args` at the same time); hook VM tests (standby `Thinking` with empty `items`, text snippet after reply, small frames carry it, terminal frame does not); service sequence (survives process-less frames, never leaks into the seal, inspect cannot regress it, binding rebuild filter); domain matrix incl. the path overrides; rail / card / sidebar specs rewritten to the persistent contract; typecheck and knip; the four `sessions-rail-activity-*` scenes re-shot with the probe. Deployment is still pending: the running desktop build ships hook v30 — the observer hot-swaps the page hook on attach, so only the desktop app needs a restart, not Cursor.

### Legacy patch, disarmed at runtime (2026-09-12)

- Question asked: can the QingTian patch's `_qtTrace` wrapper be cured for good? Read out of the bundle tail (`__QINGTIAN_SEAMLESS__`, 5,025 lines from line 59042): the wrapper is installed by a 1s poller over the 10 `__qtComposerBridge` methods and, on every call, runs `_qtTraceSafeJson(result, 900)` — a full `JSON.stringify` with a replacer and O(n²) `seen.indexOf` cycle detection — **before** it consults `_qtTraceEnabled()`; the `__QT_TRACE_CURSOR_INTERNALS=false` switch therefore silences the log and the dead-port `fetch`, not the stringify. `getStatus` / `listComposers` take summary paths; the only heavy method was `getComposerData`. That tax is already gone on 拾光's side: the inspect reads `composerDataService.getComposerDataIfLoaded` directly (in the running 15:33 build, verified in the asar), and the own gateway (previous entry in `SELF-GATEWAY-CDP-LOCATOR-RESEARCH.md`) removes every remaining bridge call. A wrapper nobody calls costs nothing.
- What the patch still costs with nobody calling it: eight `_qtSetInterval` loops — a 350ms full-DOM `querySelectorAll("button,[role='button'],a")` scan (measured 0.1ms per tick at 2,198 DOM nodes; grows with the DOM), 500ms / 700ms / 2s pollers and a 260ms command loop all `fetch`ing the dead 36530 port with backoff, two 1s installers and a watch tick — plus one dead-port POST per `_qtLog` line (91 of the last ~250 resource-timing entries). Small individually, permanent background churn in every Cursor window.
- Cure without touching files: the patch exposes `window.__qingtianSeamlessRuntime.stop(reason)` (the API the QingTian plugin used to stop an old runtime before reinstalling — clears every registered interval/timeout, sets `_qtStopped`) and every wrapped bridge method keeps its `__qtOriginal`. `cursor-legacy-patch-disarm.ts` runs one idempotent in-page expression: `stop()`, swap the wrapped methods back to their originals (leaving `bridge.__qtTraceWrapped=true` so a surviving installer short-circuits), set `__QT_TRACE_CURSOR_INTERNALS=false`, and stamp `__sgLegacyPatchDisarmed`. The observer runs it right after `locateComposerService` succeeds — i.e. only once 拾光's own service is confirmed present, so nothing 拾光 does can depend on the patch at that point — and again after every document reload (the patch restarts with the new document, so does the hook-reinstall chain). Failures are swallowed; the passive globals (`__qtComposerService`, the bridge object) stay as a transition-period fallback; on a clean Cursor the expression is a pure read.
- Not stopped, by design: the raw `setTimeout` chains inside the patch's prompt-submission flows (only started by tasks from the dead server, i.e. never) and the already-installed stop/cancel hooks (fire only on user cancel). `pollSwitch` stops too — it is the QingTian-era account switch against the dead port; 拾光's own switch pump is an independent micro-patch and is unaffected.
- Verification: VM tests of the expression against a bundle-shaped fake runtime (stop called once, originals restored, switch set, idempotent `already`, clean-page pure read, throwing `stop` tolerated); observer sequence tests (no disarm while location fails, disarm right after the successful retry, again after `executionContextsCleared` → `executionContextCreated`, a throwing disarm never blocks the hook or `connected`); read-only probe on the live workbench confirmed the preconditions (`stop` present, 10/10 wrapped methods keep `__qtOriginal`). The real disarm happens on the next desktop restart (observer attach) — this build's observer predates it. Removing the patch from the bundle for good (真摘补丁) remains a separate, one-time cleanup; its feasibility check (pristine backup vs current build) is recorded in `SELF-GATEWAY-CDP-LOCATOR-RESEARCH.md`.
- **Removed from disk (真摘, 2026-09-12 21:04)**: `cursor-legacy-patch-remover.ts` (anchor-exact pure removal + atomic writer reusing the switch-pump installer's syntax check / checksum sync / re-sign) via `scripts/unpatch-cursor.ts` deleted the four QingTian spans (75 + 106 + 34 + 240,608 chars) from the installed bundle. Post-write audit: resync diff against `.qingtian.backup` leaves exactly the four 拾光-owned inserts (usage hook ×2, profile refresh, switch pump), zero QingTian markers remain, `product.json` checksum synced (`_isPure` true again), ad-hoc re-sign passed `codesign --verify`. Rollback anchor: `workbench.desktop.main.js.sg-unpatch-backup` (pre-removal bytes). The running Cursor keeps the old bundle in memory until restart; the 15:33 desktop build still reads `__qtComposerService` — deploy the v33 gateway desktop before restarting Cursor, or restore the backup.

### Desktop main-process lag after ~1h: three measured causes, three fixes (2026-09-13)

- Method: read-only reproduction against the production data of this machine (real `state.vscdb`, real `task-pool.sqlite3`, the active run's four seats) with the production code path, plus `ps`/`top` on the running processes. The 拾光 main process (pid alive 16h) averaged **65% CPU** over its lifetime while the Cursor renderer's JS main thread was 5% busy — the desktop, not Cursor, was the bottleneck after the 09-12 hook/patch cures.
- **Cause 1 (dominant), telemetry**: `refreshTelemetry` runs every 250 ms and `readWorkspace` recomputes whenever the `state.vscdb`+WAL fingerprint changes — i.e. every tick while any agent is active. A recompute cost **272–545 ms** (≥100% of a core, back to back); 97% of it was `readTranscriptSignals → transcriptPath`: every workspace composer whose transcript is not under the path-derived project directory (most of them — Cursor 3.6 puts windows in numeric-timestamp directories; this machine has 249 project dirs, 119 numeric) fell through to `transcriptPathGlobalFallback`, one throwing `statSync` per project directory, with no cache. Fix: one `listTranscriptFiles` enumeration (≈5 ms, already run every 2 s for channel activity) builds a `composerId → newest transcript` index; `transcriptPathOf` = workspace-directory `existsSync` (new files visible immediately) else index lookup. `readComposerModelState` (411 KB applicationUser JSON) and the `composer.composerHeaders` parse (0.85 MB, 1037 headers → `parseComposer`) are memoized on the raw JSON text, so an unrelated WAL write no longer re-parses them. Stats that expect misses use `throwIfNoEntry: false`. Measured: recompute **13 ms** (≈5% of a core at 250 ms), cache hit 0.2 ms. Known trade-off: a transcript that first appears in a *non-workspace* directory is found within the index TTL (2 s) instead of immediately; the launcher's composer-binding wait is 45 s.
- **Cause 2, image attachments**: `prepareAttachments` kept the composer's `data:image/…;base64` preview URL (1–2 MB per screenshot, 3.7 MB max per message; 16 MB total on CH-1) in `attachments_json` and in the in-memory entry, and `refreshOutboundDeliveries` did `SELECT *` + `JSON.parse` over every outbound row of the scope every 250 ms (51 ms/tick = 20% of a core for a 2.3 h run with 7 images, linear in images). Fix: thumbnails are `sg-image://local/<path>` URLs (existing protocol, on-demand disk read, CSP already allows it; copy/save already fall back to `path`); pasted images without an extension are stored as `<name>.<ext>` so the protocol allowlist accepts them; the delivery sync reads only `id, delivered_at, retired_at, withdrawn_at` (`listOutboundDeliveryStateSince`). Old rows with `data:` previews keep rendering.
- **Cause 3, renderer structural sharing was dead across IPC**: `shareSnapshotStructure` reused `conversations[channelId]` by `===`, but `webContents.send` structured-clones the snapshot, so the branch never matched: every push (up to ~10 Hz while streaming) replaced every channel array → `visibleEntries` / `projectTurnTimeline` recomputed and every historical `ProcessTurnCard` re-ran `buildProcessTurnView` + `groupProcessSteps`. Fix: the main process stamps `conversationRevisions[channelId]` (same relay array reference ⇒ same number, global monotonic seed) in `getSnapshot()`; the renderer reuses the previous array when the revision matches, falls back to identity when revisions are absent (preview / old main). Live state (`liveProcess`, `liveAgentResponses`, `liveStatusLine`) changes every frame and is untouched; the process stream's hook, sealing and persistence contracts are untouched.
- **Cause 4, every push serialized the whole history**: `emit()` → `webContents.send` structured-cloned the full snapshot (≈8.7 MB for a 2.3 h run) at up to ~10 Hz while streaming, although the timeline only changes on enqueue / delivery / reply. Fix at the IPC boundary only (`register-session-ipc.ts` + `snapshot-push.ts`): a per-webContents ledger remembers which `conversationRevisions` the renderer already holds (from pushes and pull replies; cleared on `did-start-loading`), and each push drops the channel arrays the renderer has — `conversationRevisions` stays complete, `conversations` becomes a delta, `getSnapshot()` pulls stay full, in-process subscribers are untouched. The renderer's `mergeDesktopSnapshot` merges the timeline per channel by revision, independent of arrival order: local revision ≥ incoming keeps the local array, a higher incoming revision with an array replaces it, a higher revision without an array (push/pull race window) is recorded as a gap and `App` pulls one full snapshot for it (`missingConversationChannels`, once per gap); a late (older `updatedAt`) snapshot no longer overwrites sessions or live state but can still fill missing channels and never drops channels. Live state (`liveProcess`, `liveAgentResponses`, `liveStatusLine`) is pushed at full rate as before. `cursorModels` (≈82 KB, changes only with Cursor preferences; reference-stable thanks to the telemetry memo) gets the same treatment through `cursorModelsRevision`, so a live frame carries sessions + live state + revision tables only. Gaps for either section are reported by `snapshotGaps` (`conversation:<ch>@<rev>` / `cursorModels@<rev>`), keyed by revision so one gap triggers exactly one repair pull.
- Seat rotation for the never-ending Cursor turn (idle keepalive seats grow ≈4–5 bubbles/min) was decided the same day — see the next section.
- Verification: typecheck; new tests — transcript index (numeric window dirs, newest-of-many, workspace-dir precedence, TTL pickup, header/model memo), attachment projection (no `base64,` in stored rows or entries, extension fix-up, path-derived thumbnails, lightweight delivery sync with the heavy query never called), `mergeDesktopSnapshot` across `structuredClone` (same / advancing revisions, stripped push, late full pull filling a gap without regressing sessions or dropping channels, legacy snapshots), `stripKnownConversations` and the IPC ledger (first push full, unchanged channels omitted, full again after reload, pull replies recorded, legacy snapshots untouched), and an end-to-end relay → service → strip → clone → merge revision test; full suite green apart from `theme-surface-contract`, which belongs to concurrent CSS work in `controls.css` / `styles.css`; knip, build and `smoke:channel` passed. Probe scripts live outside the repo (`/tmp/sg-lag-probe/`).

### Seat auto-rotation for the never-ending Cursor turn (2026-09-13)

- **Why**: the persistent-session protocol keeps every seat's Cursor turn in `generating` forever. Measured on 4 solo seats after 20 minutes: pure-keepalive seats had grown to 82–99 bubbles / 0.7–1.1 MB (≈4–5 bubbles per 60 s keepalive: thinking + tool call + result), the active seat 148 bubbles / 1.4 MB, its document 9→18 MB. Cursor persists the whole composer and regroups the current turn on every write, so per-write cost grows linearly with bubble count and `state.vscdb` (22.7 GB) gets slower with it. There is no bug to fix on the Cursor side; the only lever is to swap the Composer before the turn gets big. v1 covers **solo seats only** (`slot.solo`, i.e. independent runs and solo seats inside team runs); team seats involve lead ownership, `team_check_in` state and task leases and are evaluated separately.
- **Safety fix that had to come first — per-round fence re-check**: `check_messages` used to verify the session token only at call start; during the long poll it took from the queue every second (`CHANNEL_POLL_INTERVAL_MS`) without re-checking. Rotating the token of an *online* seat would therefore have let the old session take a user message for up to 60 s, then have its `record_reply` refused — message consumed, reply lost, new session never sees it. `ChannelMessageService.checkMessages` now re-evaluates the fence (`ChannelCheckInput.fence`, wired by the MCP tool when a session token is present) before every queue read from the second round on; a retired session exits with `retired` within one poll interval (≤1 s) and does not refresh presence. Fail-open on fence errors (DB lock etc.), same principle as the tool layer. `prepareComposerRelaunch` was offline-only before, so this window had never been exposed.
- **Fact source — `bubbleCount`**: hook v34 frames carry `fullConversationHeadersOnly.length` (the hook already walks that array; small frames carry it too), the runtime inspect expression reports the same, both land in `CursorComposerRuntimeEvidence.bubbleCount` → `DesktopSessionService.composerBubbleCounts` (newest observation wins, excluded from the evidence fingerprint) → `AgentSession.composerBubbleCount` (rail tooltip "会话体积 N 气泡"). Transcript file size is deliberately not used as a proxy.
- **Decision** (`domain/seat-rotation.ts#seatRotationVerdict`, pure): setting on → run `running` → solo seat → session token issued → composer bound → `composerBubbleCount ≥ threshold` (default **400** ≈ 4 MB; 20–30 min for an active seat, hours for an idle one now that keepalive is 5 min) → not disabled for the channel (2 consecutive failures) → not in cooldown (**10 min** since the last attempt on this channel, success or failure) → **≥2 min** since the last rotation attempt on *any* channel (`SEAT_ROTATION_GLOBAL_SPACING_MS`, so several over-threshold seats after a desktop restart rotate spaced out instead of back-to-back) → idle now (`isSeatIdle`: online, on-duty phase and not processing, queue empty, no pending reply sync, not awaiting the user, live body not streaming — keepalive gaps count as idle so the idle clock is not reset by the model's inter-poll gap) → idle continuously for **5 min** (`SEAT_ROTATION_IDLE_MS`, tracked across snapshots per composer; a new composer restarts the clock) → no user message / reply on the channel's timeline in the last **10 min** (`SEAT_ROTATION_QUIET_MS` via `lastConversationActivityAt`, silent internal notices excluded — this is what separates "the user is chatting on this seat and just paused" from "the seat is parked"; the first cut used 60 s of idleness, which would have rotated the seat the user was actively talking to during a normal reading pause) → presence says `waiting=true` (the server sets it both inside the long poll and on the keepalive return, so this does not strictly mean "inside the call"; either way the rotated token is refused — by the per-round re-check within ≤1 s while polling, or at the start of the next `check_messages` call during the gap) → no manual launch or other rotation in flight. One seat at a time.
- **Procedure** (`application/seat-rotation-service.ts`, subscribed to session snapshots — throttled to one evaluation per second, since each evaluation takes one `TeamControlService.getSnapshot()` and streaming pushes run at ~10 Hz — plus a 5 s fallback tick): (1) read the old session token and resolve the handoff context (`SessionHandoffService.context`: composer + transcript location) *before* touching the binding — `prepareComposerRelaunch` clears `composer_id`, after which `context(channel)` cannot find the transcript; (2) `TeamControlService.prepareComposerRelaunch(channel, { allowIdleOnline: true })` — the new option admits online seats that are waiting with an empty queue, no pending reply and no user question; `undefined` means the seat just stopped being idle → give up silently, reset the idle clock, no failure counted; (3) `deliverFrom(context, self, note, { holdSessionToken: oldToken })` — the hold must be the **old** token (the current one now belongs to the new session; `holdUntilNewSession` would resolve to it and lock the new session out); a delivery failure does not roll the token back (that would resurrect the old session) — the launch proceeds and the outcome message says the context was not delivered; (4) `AgentSessionLauncher.launch([channel], progress, { origin: 'seat-rotation' })` — the existing one-click chain (start prompt with the new binding marker → CDP composer → telemetry-confirmed binding → on-duty), progress pushed on the same `agentLaunchProgress` IPC as manual launches; the plan carries `origin` so the main-process wiring does **not** feed it to `AccountAutomationService.onAllSessionsTriggered` (the post-launch account automation — 奥仔 processing / hardening / account switch — is irreversible and follows only the user's manual batch launch). Result → `DesktopSessionService.noteSeatRotation` → `AgentSession.seatRotation` (`rotating` / `done` / `partial` = new session ready but the handoff message could not be enqueued, so it has no context — amber, persistent / `failed` / `disabled`), rendered as a pill next to the queue badge in the rail (`done` only during the cooldown; the others until replaced or the run changes). Two consecutive failures disable the channel; turning the setting off clears finished notices, turning it back on also clears disabled/cooldown state. Run change clears everything.
- **Settings**: `userData/seat-rotation.json` (`SeatRotationSettingsStore`, default `{ enabled: true, bubbleThreshold: 400 }`, threshold 100–2000 in steps of 50; idle / quiet / cooldown / spacing are constants, not settings); IPC `seat-rotation:get/save-settings`; settings page → 自动化 → "席位自动轮换" (toggle + threshold slider). No confirmation dialog by design: rotation happens while the seat is idle and a dialog would likely be answered after the seat has started working again; the rail pill and the handoff message + "已接手" reply in the timeline are the visible trail.
- **Known residual race**: a user message enqueued in the few milliseconds between the token rotation commit and the old session's fence re-check could still be taken by the old session (its `record_reply` is then refused). The window is the few milliseconds between a round's fence check and its queue read (both inside one poll round); it requires the user to send exactly then — not worth a lock in v1.
- Verification: typecheck; new tests — `seat-rotation.test.ts` (settings normalisation, idle predicate incl. keepalive gaps, every skip reason of the verdict, notice visibility/labels, store round-trip, service orchestration: ordered happy path with old-token hold, cooldown / one-at-a-time / launcher-busy, restart of the idle clock on composer change, silent abort when `prepareComposerRelaunch` refuses, missing transcript / token, delivery failure without rollback, failure → cooldown → second failure → disabled → re-enable, non-solo / non-running / streaming, keepalive-gap trigger deferral, run change, start/stop), `team-control-service.test.ts` (`allowIdleOnline` admits only online+waiting with empty queue and no pending reply / question, rotates token and binding key), `session-handoff-service.test.ts` (explicit hold token for self, ignored for other channels), `desktop-session-service.test.ts` (`noteSeatRotation` projection, same-value no-op, run-change clear), `session-rail-card.test.tsx` (pill states, cooldown expiry, bubble-count tooltip), `settings-page.test.tsx` (section render / collapse), `register-seat-rotation-ipc.test.ts`; plus the earlier per-round fence tests (`channel-message-service.test.ts`, `channel-mcp-server.integration.test.ts`) and hook/inspect `bubbleCount` tests (`cursor-stream-observer.test.ts`, `desktop-session-service.test.ts`). Full suite 172 files / 1704 tests green; knip, build and `smoke:channel` passed.

### Plan panel refinement: one todo language for timeline and inspector (2026-09-13)

- Why: the inspector's 计划 tab was the rough one of the app's **two** todo renderings. The timeline todo block already had the Cursor-native design (SVG stroke check, inverted solid-circle spinner, per-task segmented progress, row fade-in) while the plan panel used a text-glyph `'✓'`, a generic border spinner, a uniform 3px bar, and marked the current row four times over (info wash + inset bar + `进行中` label + spinner) with the section-header hint repeating the running item's full text a fifth time. Long technical items (the panel's real content) wrapped 2–4 lines with `overflow-wrap: anywhere` cutting paths at arbitrary characters, and completed items kept multi-line strikethrough — the noisiest element of the list. A written-but-unused `transition: background` betrayed a missing hover rule.
- Shape: `TodoIndicator` + `todoTone` are extracted to `src/renderer/src/TodoIndicator.tsx` and shared by `ProcessTurnCard` and `PlanPanel` (single source for the four-state glyphs; `cancelled` gains a slash-circle distinct from `pending`, `running` is normalised as an alias of `in_progress`). The panel reuses the timeline's `.todo-progress` segmented bar (one segment per todo, flowing in-progress segment) in place of the uniform bar; the header hint is a calm source note (实时 / 最近一轮), the ratio stays in the aside. Current row = spinner + 2px info-colored left inset + full-strength text, nothing else; `aria-current="step"` unchanged. Completed / cancelled rows clamp to one line (`-webkit-line-clamp`, full text in `title`) with muted single-line strikethrough. ASCII paths / known file extensions / backtick spans render as mono `<code>` (`todoContentSegments`, ASCII-only classes so Chinese slash-prose is never mistaken for a path). Rows get the missing hover wash; fs-11 → fs-12 with `break-word`.
- Motion, one-shot by construction: a render-time tone memory (`refreshToneMemory`, keyed `index:content`, recomputed only when the items reference changes — mount and whole-list replacement do not mark) tags `is-just-completed` / `is-just-started`; CSS draws the check via `stroke-dashoffset` (260ms) and pops the spinner (180ms); rows fade in 200ms; the current row keeps itself visible via `scrollIntoView({ block: 'nearest' })` guarded for jsdom. All of it is switched off under `prefers-reduced-motion`, with the check's dash state reset so the glyph cannot get stuck invisible.
- Verification: typecheck, knip, build; new `plan-panel.test.tsx` (tone normalisation incl. the running alias, segment classes + progressbar aria, single current-row marking with no `进行中` label, settled-row tooltip + code tokens, one-shot animation marks across re-renders, empty state), `inspector-shell.test.tsx` updated to the new header contract; suite green apart from the concurrent seat-rotation work-in-progress. Preview: new `?plan=long` scene (10 real-granularity items, all four tones) with `plan-long-{light,dark}` shots; `plan-{light,dark,empty}` re-shot.

### Keepalive window 60 s → 5 min, with the Cursor ceiling measured (2026-09-13)

- **Why**: every `check_messages` keepalive return costs one model turn (thinking + a new tool call + result), i.e. 4–5 bubbles per cycle on a seat that is doing nothing; seat rotation (above) caps the damage, a longer window slows the growth at the source. Delivery latency is unaffected: the queue is polled every second inside the long poll and a message returns immediately.
- **Ceiling, read from Cursor itself** (read-only, `/Applications/Cursor.app/Contents/Resources/app/extensions/cursor-mcp/dist/main.js`, `@modelcontextprotocol/sdk` pinned to 1.25.1 in its `package.json` overrides): the tool-call path is `client.callTool({ name, arguments, _meta: { progressToken } }, CallToolResultSchema, { onprogress, timeout: 36e5 })` — a **1-hour** per-request timeout, `resetTimeoutOnProgress` left at its default `false`, no `maxTotalTimeout`. The workbench side (`MCPService.callTool` / `callToolForLease` → extension host) only threads an abort signal; its `mcp_ipc_timeouts` experiment config covers metadata / lifecycle / dashboard / recovery commands, not tool calls. So the old 60 s was never a client constraint.
- **Change**: `CHANNEL_KEEPALIVE_TIMEOUT_MS` = 300 000 (12× under the ceiling). Override without rebuilding via `env.SG_TEAM_KEEPALIVE_MS` in `mcp.json` (`resolveKeepaliveTimeoutMs`: integer ms, clamped to [10 s, 30 min], anything else falls back to the default); the MCP process logs the effective value on startup (`ready unified (keepalive 300s)`). The desktop's idempotent `mcp.json` registrar (`global-mcp-registrar.ts`) now carries a hand-written `SG_TEAM_KEEPALIVE_MS` over when it rewrites the entry — previously any extra env key was dropped on the next start (and the rewrite itself made Cursor reload the server).
- **Coupled constant**: the telemetry "zombie poll" cap (`CHANNEL_POLL_SILENCE_MS`, channel-level transcript silence while the transport claims waiting/keepalive) is now derived as `CHANNEL_KEEPALIVE_TIMEOUT_MS + 60 s` instead of a hard-coded 60 s. Measured the same day, though, the premise behind that heuristic does not hold for Cursor 3.6 persistent sessions: transcripts are flushed when the turn *ends*, not per tool call — all six healthy waiting seats of the current run had transcripts frozen at their 1 KB opening message for 70 minutes. The verdict therefore fires for every long-lived seat regardless of the cap; it is harmless (`verifyAgentRuntime` only annotates while the MCP heartbeat is fresh, never flips such a seat offline) but effectively dead weight — candidate for removal, not done here. Everything else that looked like a 60 s dependency was checked and is independent of the window: presence staleness (`lastSeenAt` is touched every poll round), the composer-level `check_messages → waiting` verdict (no age limit), the launcher's on-duty wait, the per-round fence re-check, hook noise filtering (by tool name), seat-rotation idle timing (60 s of idleness, not "one cycle").
- Trade-offs accepted: `storage_unavailable` is reported after a full window instead of one minute (the retry streak still stops after 3 windows); Cursor's chat shows the `check_messages` tool running for up to 5 minutes instead of 1 (cosmetic, same as before in kind). Not changed: `CHANNEL_POLL_INTERVAL_MS` (1 s) and `CHANNEL_REPLY_SYNC_STALE_MS` (290 s).
- Verification: `channel-protocol-policy.test.ts` (default value, env override parsing incl. clamps), `cursor-composer-telemetry.test.ts` (zombie cap follows the window: silence of window + 30 s is `unknown`, window + 61 s is `stopped`); existing long-poll suites use injected short windows and are unaffected.

### Windows hand-off readiness: reserved CDP port, ASCII executable, NSIS artifact (2026-09-13)

- **Context**: a colleague on Windows asked for a build. Code-level support exists (platform branches, injected unit tests, `windows-latest` in CI). The last run on real Windows hardware was 09-06 (commit d24b254): the packaged `win-unpacked` app started, migrated its data directory, registered itself in `~/.cursor/mcp.json` and passed the packaged stdio smoke; the CDP-enable restart failed there on an all-users install and was fixed the same day (b650d87) without re-verification. Nothing after that — self-gateway session creation, the 09-12 hardening, today's three items, the NSIS installer — has run on a Windows machine. The 09-12 "real Windows blocker" (`window.__qtComposerService` from the QingTian-era patch) no longer applies by design: session creation, inspect, question answering and warmup go through the self-mounted `__sgComposerService` found via pure CDP `queryObjects` (`cursor-composer-service-locator.ts`), which has no platform assumptions — still unverified on Windows hardware. Three items approved and done this round; the rest of the risk list (Cursor version pinning, Gatekeeper-equivalent SmartScreen, antivirus on bundle writes) is recorded in the CH-1 analysis reply, not here.
- **Reserved CDP port** (`infrastructure/cursor/cursor-cdp-port.ts`): Windows reserves TCP ranges for Hyper-V / WSL2 / Docker; a Cursor started with `--remote-debugging-port=9333` inside such a range fails silently and the whole CDP chain (session creation, process stream, keeper auto-heal) goes dark with no error pointing at the port. The desktop now resolves the port once at startup: `netsh int ipv4 show excludedportrange protocol=tcp` (parsed language-independently — data rows are `start end [*]`, headers vary), and if the preferred port (`SG_TEAM_CURSOR_CDP_PORT` or 9333) is inside a range it walks upward to the first free port (≤256 attempts, else stays and says so). Non-Windows returns the preferred port untouched; a failing probe never blocks startup. The result feeds `new CursorCdpSessionCreator({ port })`, and every consumer (stream observer, keeper, relaunch args, account switcher) already reads `cursorCdpCreator.debugPort`, so the moved port is used consistently; the relaunch result message and the startup log explain why the port is not 9333.
- **ASCII executable**: `build.win.executableName = "ShiGuang"` — `mcp.json`'s `command` path no longer contains CJK (`…\shiguang-team\ShiGuang.exe`), avoiding encoding/quoting hazards when Cursor spawns the stdio server; product name and shortcuts stay 拾光; the installer artifact is `ShiGuang-Setup-<version>.exe` (ASCII too, since it becomes a GitHub Release download URL — see the next entry). Install directory: electron-builder itself only uses `productFilename` for the NSIS/MSI install dir when it is pure ASCII (`getWindowsInstallationDirName`, `/^[-_+0-9a-zA-Z .]+$/`), otherwise the sanitized package `name` — so the default per-user path is already `%LOCALAPPDATA%\Programs\shiguang-team\`; no extra option needed. `scripts/verify-built-mcp.ts --packaged` and `docs/TASK-MCP.md` follow the new name. Mac binary name untouched (changing it would change `process.execPath` and force an `mcp.json` rewrite for existing installs).
- **Installer + CI artifact**: `pack:win` now builds the NSIS installer (`electron-builder --win --x64`; the former `--dir` variant is `pack:win:dir`, and `win-unpacked/` is still produced as the intermediate so `verify:win` / `smoke:mcp:packaged` are unchanged). New CI job `package-windows` (needs `verify`, runs on `windows-latest`, only on `workflow_dispatch` or pushes to `main`) uploads `release/*.exe`, `*.exe.blockmap`, `latest.yml` as artifact `shiguang-windows-x64` (14 days). Cross-packaging Windows from the mac is deliberately not used.
- **Verification**: typecheck; `cursor-cdp-port.test.ts` (real zh-CN `netsh` output shape — localized headers, admin `*` rows, malformed lines skipped; in-range / out-of-range picks, walk-up over reserved ranges, exhaustion, env override, non-win32 passthrough, probe failure, notice texts); `electron-builder` schema check that `win.executableName` exists; CI YAML parsed. Full suite 176 files / 1752 tests (one pre-existing timing-sensitive launcher test, `全部提交成功即触发 onAllTriggered`, failed once under full-suite load and passes 4/4 in isolation — not touched by this work); knip clean.

### Settings › 统计: the usage ledger as a page, without a hero band (2026-09-13)

- **Why**: all cost/token data already existed (`CursorUsageSnapshot` per composer with the V3 per-turn ledger, seat bindings, model prices) but was only visible one session at a time in the usage popover. The brief was a "control-room" overview that stays inside the light, calm language of the app; the KPI hero band was rejected as generic — the data itself has to carry the page.
- **Shape**: a 7th settings group (`#account:stats`, `SettingsPage` → `SettingsStats`, mounted with `hidden` like the other groups). Renderer-only projection: `settings/stats-view.ts` (`buildSessionStatsView`: range `today/7d/30d` → hour or day buckets, four-bucket token split reusing the popover's exclusive-bucket rule, per-seat cache-hit rate, model rows, session rows with the quality badge 精确/混合/估算/旧账) — no new IPC, no chart library (div/CSS bars; runtime deps stay `ws` + `zod`). Page = control row (range + page-level metric 成本|Tokens — the toggle only changes the rendering yardstick, the view model is metric-agnostic) → 14 px **spectrum band** (one segment per seat, click = filter the whole page, chip to clear) + ledger line → cost/token rhythm (stacked bars, hover detail, keyboard ←/→/Home/End with an `aria-live` readout placed outside the `role="img"` subtree) → token composition + per-seat rows → model distribution (folds after 6) → session table (numeric headers right-aligned on the `tabular-nums` axis, four sortable columns, folds after 20).
- **Decisions**: composers with no live seat binding (rotated / previous runs) fold into a grey 「历史会话」 group instead of disappearing; a hidden panel freezes its last view (`frozenView`, no recompute on the ~10 Hz usage pushes while another group is open, fresh data on the activating render); seat projections are memoised in `App` so the view model only recomputes on usage pushes / range changes; task and message throughput is deliberately postponed until the dynamic-groups refactor lands (it would rebuild the team model underneath). Known trade-offs: an in-progress turn migrates to the latest bucket as it is observed (ledger semantics); a filter kept across a range change may point at a seat with no data in the new range (band dims, cards show their empty states, the chip clears it).
- **Verification**: `stats-view.test.ts` (bucketing, attribution incl. history, savings, filters, sorting, empty), `settings-stats.test.tsx` (metric toggle, header alignment, keyboard roaming, hidden-freeze, deep link); preview `?stats=1` (`settings-stats-{light,dark,7d-filtered-light,tokens-dark}`).

### Theme accent presets: one base colour, a derived chain, curated not free (2026-09-13)

- **Why**: users expect an accent choice; the theme pipeline was already one variable away from it — `--anthropic-orange` is defined once (`claude-theme.css`), every brand use goes through it or a `color-mix()` derivative (`--accent-soft/wash/border*`, `--surface-tint`, `--shadow-brand`). Three hard-coded bypasses were closed on the way: `--accent-deep` / `--accent-bright` (now per-preset `light-dark()` anchors) and `--color-ring-primary` (now `color-mix(var(--accent-bright) 72%, transparent)`, fallback = the factory ring).
- **Shape**: `appearance-preferences.ts` — `ACCENT_PRESETS` (拾光橙 default, 黛蓝, 竹月, 洛神紫, 胭脂, 墨玉; each `base` + `deep[light,dark]` + `bright[light,dark]`, hand-checked contrast: every non-default preset's deep-on-wash ratio is above the factory orange's in both modes), `accent` joins the v1 preferences JSON (`normalizeAccent` folds unknown ids and old records without the field back to the default). `applyAppearancePreferences` writes the three overrides inline on `<html>` for a non-default preset and **removes** them for the default, so the stylesheets stay the single source of truth; it runs before React mounts (`main.tsx`), so there is no flash. UI: a 主题色 row in the appearance popover (`AppearanceSettings`, swatches carry `--swatch`, active = double ring + `aria-pressed`, the active preset's name is written at the row's right end in its deep tone, like 卡片透明度 · 90%).
- **Rules that do not follow the accent**: status colours (green / blue / amber / red / grey) — unchanged; **data category palettes** — the four token buckets and the stats seat palette. This surfaced one real defect in review: the Output bucket used `--anthropic-orange`, so under 黛蓝 it collided with Input (info blue) and under 墨玉 with the two cache greys. Pinned as `--usage-output: #ff6b35` (`styles.css`, used by the usage popover and the stats page). Single-series bars (model distribution) and selection states (spectrum segment, filter chip, nav) do follow the accent, by design.
- **Not done, by choice**: a free colour picker (needs a runtime contrast guard and collides with the status hues — a curated palette avoids both), background selection / motion / density switches (assessed as cheap follow-ups, not part of this round), window chrome (neutral on both platforms, nothing to change).
- **Verification**: `appearance-settings.test.tsx` (normalisation + backward compatibility, apply/remove of the three overrides, preset invariants, swatch row incl. name and unknown-id fallback, hidden row for callers without `onAccentChange`), `theme-surface-contract.test.ts` unaffected; preview scenes `appearance-accent-popover-light` (洛神紫, popover open) and `settings-stats-accent-violet-dark` (whole page re-dressed, Output legend still orange); ad-hoc shots under 黛蓝 / 墨玉 for the stats page and the session rail confirmed the pinned bucket colour and that the rail only re-colours its selected-row bar.

### Hand-off pipeline: ad-hoc re-sign, ditto zip, tag-driven GitHub Release (2026-09-13)

- **Why**: "how do I give this to a colleague, simply". Measured on the existing `release/mac-arm64/拾光.app`: electron-builder with `identity: null` skips signing entirely, so the bundle carries only Electron's prebuilt `linker-signed adhoc` main executable (identifier still `Electron`) and `codesign --verify --deep --strict` fails (`code has no resources but signature indicates they must be present`). It runs locally only because locally built files carry no `com.apple.quarantine`; after AirDrop / WeChat / cloud drive / browser download Gatekeeper evaluates the bundle, the signature is invalid and macOS shows "已损坏，无法打开" — the variant with **no** "仍要打开" escape hatch (Control-click → Open is gone on macOS 15 as well). Only removing the quarantine flag works. A second, silent trap: the `.app` contains 14 symlinks (Electron Framework `Versions/Current` etc.); `zip -r`, exFAT sticks and Windows shares flatten or expand them.
- **`scripts/dist-mac.mjs`** (`npm run dist:mac` = `pack:mac` + this): `codesign --force --deep --sign -` then `--verify --deep --strict`, then `ditto -c -k --keepParent` → `release/ShiGuang-<version>-mac-<arch>.zip` (arch from the electron-builder dir name; ASCII file name for URLs). Verified on a copy of the current build: identifier becomes `app.shiguang.team`, verify passes, 14 symlinks survive the zip round trip (128 MB). Gatekeeper still rejects ad-hoc code (`spctl: rejected`), but the dialog becomes "无法验证开发者" with the System Settings → "仍要打开" route; the one-liner `xattr -dr com.apple.quarantine /Applications/拾光.app` works in both cases. Fully silent installs need Developer ID + notarization — not done. Never run it against the build that is currently running: `~/.cursor/mcp.json` points at `release/mac-arm64/拾光.app`, whose executable hosts the live MCP server; re-signing rewrites the binary.
- **`.github/workflows/release.yml`**: on tag `v*` — `mac` (macos-latest = Apple Silicon runner: `npm ci`, tag == `v<package.json version>` guard, `typecheck`, `dist:mac`) and `windows` (`typecheck`, `pack:win` → `ShiGuang-Setup-<version>.exe`) in parallel, then `publish` (ubuntu, `gh release create --notes-file .github/release-notes.md`, or `gh release upload --clobber` when the release already exists). `publish` needs only the mac job to succeed: the Windows installer has never been produced on any machine, so its failure must not block the mac hand-off — the job log prints `needs.windows.result`. `workflow_dispatch` produces artifacts only. `ci.yml`'s `package-windows` job stays as the per-push smoke of the NSIS build. The repository (`lyr339/SG-Team-for-Mac`) is public, so recipients need only the link; `.github/release-notes.md` carries the recipient instructions (quarantine one-liner, SmartScreen, Cursor 3.6.31 + "关闭 Cursor 自动更新", first-launch CDP restart prompt, MCP auto-run, `mcp.json` is written automatically).
- **Prerequisite the pipeline cannot check**: local `main` is 18 commits ahead of `origin/main` and today's work is uncommitted; the first release needs commit + push + tag. Cursor 3.6.31 on the recipient's machine is a hard requirement (locator, stream observer, status line, usage and switch pump are all reverse-engineered against it) and nothing in the app verifies it yet — candidate follow-up: read Cursor's version at startup and warn.
- **Verification**: `dist-mac.mjs` end-to-end on a `/tmp` copy of the release app (re-sign, verify, ditto, extract, symlink count, `codesign -dv`); `release.yml` parsed (jobs `mac`/`windows`/`publish`, triggers `push.tags v*` + `workflow_dispatch`); knip (script is an entry via `scripts/*.{ts,mjs}`).

### Windows CDP restart: taskkill exit code, cmd quoting, three-way diagnosis (2026-09-13)

- **Trigger**: the user remembered "9333 had a problem on Windows — it was not picked up, or something". Reading `cursor-cdp-restart.ts` / `cursor-windows-launch.ts` against Windows semantics (nothing here has run on a Windows machine since 09-06) found two defects and one blind spot in exactly that path.
- **taskkill exit code**: the graceful quit is `taskkill /IM Cursor.exe` (no `/F`, on purpose — WM_CLOSE keeps unsaved work). An Electron app has a dozen same-named windowless child processes (renderer, GPU, extension host, pty); for each of them taskkill prints `This process can only be terminated forcefully` and exits non-zero, so `execFile` rejected and the whole restart returned `重启 Cursor 失败：…could not be terminated` — while the main window had received WM_CLOSE and Cursor was in fact quitting. Whatever the user did next (relaunch Cursor by hand, click again) looked like "the port was not carried along". Now taskkill's exit code is ignored; the process-count wait (12 s) is the only verdict, and its stderr is appended to the timeout message when the count never reaches zero.
- **cmd quoting**: the launch was `execFile('cmd.exe', ['/d','/s','/c', 'start "" "<exe>" "<ws>" --remote-debugging-port=N'])`. Without `windowsVerbatimArguments`, libuv wraps the fourth argument in quotes and escapes the embedded quotes as `\"`; cmd does not understand backslash escapes, so `start` received `\"\" \"C:\…\Cursor.exe\" …` and treated `\"\"` as the program. `cursorWindowsStartCommand()` now returns file + args + `{ windowsVerbatimArguments: true }` with the whole start line wrapped in one pair of quotes (cmd's `/s` rule strips exactly those) — the same shape Node itself uses for `shell: true` — and both callers (CDP restart, cold account switch relaunch) pass the options through; the switcher's injectable `execFn` gained the optional third parameter. The 09-06 report "`start "" Cursor.exe` could not find the file" was most likely this defect wearing a different message.
- **Diagnosis instead of "not ready"**: when the port has not answered after 30 s on Windows, `windowsCursorCommandLines()` (PowerShell `Get-CimInstance Win32_Process -Filter "Name='Cursor.exe'"`, UTF-8 prelude) feeds `describeWindowsCdpStartFailure()`: no process → Cursor did not start (message names the executable used); processes without `--remote-debugging-port=N` → a leftover instance took over the launch, quit Cursor fully; processes with the flag but a silent port → reserved range (`netsh … excludedportrange`) or security software, switch ports via `SG_TEAM_CURSOR_CDP_PORT`; PowerShell failure → generic. macOS keeps its previous message.
- **Verification**: `cursor-cdp-restart.test.ts` — the fake `taskkill` now throws with code 128 like the real one (the existing graceful-quit case still succeeds, and asserts verbatim options + the exact quoted command line), new cases for "processes never exit" (timeout text carries taskkill output, no second instance) and the four diagnosis branches; `cursor-windows-launch.test.ts` — command shape, command-line probe parsing/failure, three-way classification. Still to be confirmed on a Windows machine by the user.

### First tagged release v0.2.0 and what the runners taught (2026-09-13)

- **Outcome**: `v0.2.0` published from `release.yml` — `ShiGuang-0.2.0-mac-arm64.zip` (127 MB; verified after download: `codesign --verify --deep --strict` passes, identifier `app.shiguang.team`, 14 framework symlinks intact, Info.plist 0.2.0, `Resources/mcp/index.mjs` present) and `ShiGuang-Setup-0.2.0.exe` (112 MB, NSIS 3.04 self-extracting PE, `asInvoker`). Whole pipeline: mac job 62 s, windows job 112 s, publish 14 s.
- **Six CI facts learned on the way, all fixed in `main`** (25 pushed commits had never run on the runners before 09-13):
  1. Runners are UTC; tests asserting local-time labels were written in UTC+8 → `vitest.config.ts` pins `TZ=Asia/Shanghai`.
  2. Seven tests fed mac-style paths to Windows → fixtures now take platform-native forms (config path `C:\…`, probe `uri.path` `/C:/…`); product code was consistent all along.
  3. Actions logs on a public repo need a login, annotations do not → packaging steps tee their output and emit the last 30 lines as `::error` on failure (also uploaded as `pack-win-log`).
  4. `npm ci` on the runner does not produce `node_modules/electron/dist` (npm ≥ 12 skips dependency install scripts; `allowScripts` matching is still moving) → packaging jobs run `node node_modules/electron/install.js` explicitly.
  5. `ELECTRON_MIRROR=npmmirror` from a GitHub runner did not finish downloading Electron in 15 min → removed on CI (GitHub Releases is the fast source there; the README keeps the mirror as advice for slow local links); `concurrency: cancel-in-progress` and `timeout-minutes: 30` bound the damage of a hung packaging job.
  6. electron-builder with `CI=true` infers a GitHub publish target from the git remote and fails on a missing `GH_TOKEN` after the installer is already built → every `pack:*` script passes `--publish never`; publishing is our own `gh release` step.
  Plus: windows-latest occasionally needs >5 s to create temporary `node:sqlite` databases (five timeouts on a commit that had passed twice) → `testTimeout: 20_000`, `retry: 1` on CI only.
- **Not verified**: the installer on a real Windows machine (user-owned step); the mac zip was only structurally checked, not launched (a second instance would collide with the running app's single-instance lock).

### Cache Write is a provider fact, not a Claude-shaped ratio (2026-09-15)

- **Symptom**: the usage popover showed a "Cache Write" bucket for Kimi K3 and for every other model that has no such concept. Root cause, read from the code: every estimated turn is split by a fixed ratio table (`USAGE_PROFILES`, taken from Claude-shaped screenshots — `default` = 0.5 % fresh / 7.4 % write / 92.1 % read), so the estimate manufactures a write share for GPT-5.5 and earlier, Gemini, Composer, GLM, DeepSeek, Qwen and Muse Spark alike; the 09-13 fix (`5a8d8a6`) only special-cased Kimi with a `/^kimi\b/` regex on the price label, and the four-row list rendered "Cache Write 0" even then. Costs were never wrong — those providers' `cacheWritePerM` equals `inputPerM`, so the split changed nothing in dollars — the defect is semantic: presenting a bucket the provider does not have. All five turns in the local ledger were estimates (the persistent Cursor turn never fires `turnEnded`), so the split rule is the whole picture the user sees.
- **Provider check (official pricing pages, 2026-09-15)**: only Anthropic (`cache_creation_input_tokens`, 1.25×) and OpenAI GPT-5.6 / GPT-6 (`cache_write_tokens`, 1.25×) bill a write bucket. OpenAI ≤ 5.5 ("no additional cache-write charge"), Gemini implicit caching (creation at input price; explicit caching has storage rent, not a bucket — Cursor does not use it), Cursor's Grok / Composer ("Cache write —"), Moonshot Kimi (hit / miss only), Z.ai GLM, DeepSeek, Qwen and Meta Muse Spark have two-state input only.
- **Rule**: `hasCacheWriteBucket(price) = cacheWritePerM > inputPerM`. No new field — the table already encodes the fact as the 1.25× premium, and persisted `UsageTurn.price` objects (validated numeric by `cursor-usage-store`) need no migration. `referenceUsage` folds the write share back into fresh input for bucket-less providers (read and total input unchanged, cost identical); `projectUsage` zeroes the fabricated write on **estimated** turns of such providers (`hasFabricatedCacheWrite`) and `upgradeUsageEstimate` now routes through it whenever any such turn exists, so historical Gemini / GPT / Composer ledgers are normalised on the first load and stay idempotent afterwards; exact Cursor settlements are never touched. Presentation: `usageHasCacheWriteBucket(usage)` (any ledger turn's provider has the bucket, or the data carries writes, or — for V2 ledgers without turns — the priced model does) decides whether the popover row / breakdown segment / `cursorUsageDetail` text exist at all; a premium provider at 0 keeps the row (0 is an observation, "no bucket" is not). Unknown models still estimate as the Sonnet tier and therefore keep a write bucket — consistent with the label "· Sonnet 档估算" they already carry.
- **Price table** (snapshot 2026-09-04 → 2026-09-15): added GPT-6 Astra / GPT-6 ($10 / $50 / read $1 / write $12.5 — previously fell into the `gpt` generic at $1.25, 8× under), Composer 1 ($1.25 / $10 / $0.125 — was hitting the `composer` generic), Muse Spark 1.3 ($1.25 / $4.25 / read $0.15 — was Sonnet tier, 2.4× over); DeepSeek collapsed to one `deepseek` entry at V4.1 Flash off-peak ($0.15 / $0.60 / hit $0.003) because DeepSeek bills `v4-pro` requests as Flash from 09-14. Everything else re-checked against Cursor's Models & Pricing page unchanged.
- **Not done, by choice**: the stats page (`SettingsStats`, four-bucket legend, per-model and per-row tooltips) still lists Cache Write — it is an aggregate over sessions and models, and the file is under concurrent edit by another agent this round; the estimate ratios themselves remain screenshot-derived (Anthropic cost is sensitive to the write share: 24 K write tokens were 45 % of a $0.68 Fable turn), a limitation of the method rather than a bug.
- **Verification**: `cursor-usage.test.ts` — provider matrix for `hasCacheWriteBucket`, bucket-less estimates keep read / total / output of the same-ratio GPT-5.6 shape and the same cost, mixed frozen ledger (Kimi + Gemini estimate zeroed, Gemini exact and Claude untouched, source object intact, re-projection idempotent), `upgradeUsageEstimate` on a persisted `default` Gemini turn (normalised once, `toBe` on the second load, Claude passthrough), `usageHasCacheWriteBucket` / `cursorUsageDetail` cases incl. V2 labels; `session-usage-stat.test.tsx` — Kimi K3 renders three rows summing to the total, no `is-cachewrite` segment, aria detail without Cache Write.

### Session pool + collaboration groups, phase 1 (2026-09-14 → 09-15)

- **Why**: with one active run per workspace, a team run and an independent batch were mutually exclusive — forming a team ended every independent session (`replaceActiveRun` → `beginScope` retired all presence), adding a seat rebuilt the whole run and rotated every token, and roles were frozen at run creation because the launch hint is the Composer's first message. Sessions live for hours and collaboration is needed *between some of them, for a while*; restarting Composers to regroup throws away the very context the collaboration is about. The stage-0 experiment (09-13, four hand-written rows in the live database) proved a running independent session can join and leave a group without a restart, and surfaced the two blockers this phase removes: the registration capability snapshot (`[]` for solo seats) vetoing every grouped authorization (`agent_capability_mismatch`), and `TeamFailoverService` treating grouped seats going offline as "team over".
- **What** (branch `feat/dynamic-groups-phase1`, task book `.handoff/DYNAMIC-GROUPS-HANDOFF-TODO.md`, decisions §14): schema v7 → v8 (`team_groups`, `team_group_events`, `agent_slots.group_id / home_role_id / group_joined_at`, `team_roles.group_id`); repository group transactions returning the membership delta; identity → `groupId` / `not_in_group`; `group_id` snapshots on tasks, messages, threads and run-scoped memory with per-group Agent views; orchestrators per group; pool-safe failover and handoff; `channel_outbox.kind` with the membership suffix; group-aware `team_check_in` briefing and `team_run`; `TeamGroupService` + `team-group:*` IPC; `RunGroupsPanel`. `registerSeat` / `addSeats` / `removeSeat` deliberately deferred. Two implementation gaps found by the tests along the way and fixed in the same slice: `buildGroupRoles` numbered instances by count and collided with `UNIQUE(run_id, role_key)` after a mid-list departure (now continues from the group's max), and `releaseAgentWork` only un-targeted tasks the leaver was holding, leaving a queued task planned *for* that seat unclaimable by anyone (now every non-terminal task aimed at the leaving seat is un-targeted, event `task.untargeted_by_membership`).
- **Verification**: repository / identity / failover / task-pool / collaboration / memory / orchestration / protocol / relay / service / IPC / run-view / panel tests (see task book §13 for the per-slice files), plus `tests/channel-mcp-stdio-groups.integration.test.ts`, which spawns the real `src/mcp/index.ts` over stdio against the same SQLite file and walks solo `not_in_group` → create group → membership notice through `check_messages` → grouped briefing → task isolation between two groups → remove → `not_in_group`, asserting the three `session_token`s and the fence verdicts never change. Real-machine acceptance (task book §11) is the user's step and has not been run.

### Generated images in the process stream: `generateImageToolCall` gets a presenter and an `image` field (2026-09-15)

- **Symptom**: a turn that called Cursor's image generation four times showed four opaque cards in 拾光 — heading `generate_image` (the raw tool name), no object, no state verb, and the images nowhere; expanding gave two JSON blobs. Cursor's own chat showed a "Generated image" card with the picture inline.
- **Root cause, three sources cross-checked** (persisted `process_blocks_json`, the raw bubble in `state.vscdb`, and Cursor's workbench bundle): the hook's `TOOL_PRESENTERS` had no entry for `generateImageToolCall` (Cursor tool enum 53), so the block fell to the `other` bucket — `classifyTool('generate_image')` matched no keyword, `toolSummary` looked for `path/file_path/…/filename` and missed the camelCase `filePath`, the view used the raw tool name as the heading. The result `{filePath, imageData}` went through `safePlain`, which (correctly) replaces the 1–2 MB base64 with `[binary/image payload omitted]`, and `outputText` then serialised the whole object as text — leaving the only useful fact, the file path, inside a JSON string no later layer parsed. The renderer had no image form for tool blocks at all: the local-image capability (`sg-image://local/<path>`, `MessageImage`, lightbox) was wired only to reply markdown and user attachments.
- **How Cursor does it** (bundle facts): verbs `Generating image / Generated image`; result type `agent.v1.GenerateImageSuccess { filePath, imageData }`; the card renders `<img>` from `imageData` as a data URL when present and falls back to `uriToBrowserUri(file(filePath))`; the serialiser (`T6n`) clears `imageData` on persistence, so after a restart Cursor itself relies on the file. Hence the design decision here: **the file path is the only image source**; base64 is never carried through frames, IPC or SQLite.
- **Shape**: `ProcessToolKind` gains `image`; `ProcessBlockTool.image?: { path }` (domain, `conversation-entry.ts`). Hook v35 adds `generateImageToolCall: { kind: 'image', summary: basename(filePath) }` and extracts the path from the modern payload or the on-disk `{"success":{"filePath"}}` string (`generatedImage`); image blocks carry `output: ''` (the picture is the output). `parseProcessStream` validates `image.path` for `toolKind === 'image'` only; `DesktopSessionService` passes it through and includes it in the process fingerprint. Renderer: verbs 生成图片中 / 已生成图片 / 生成图片失败, file name as the object, `ImageBody` under the card head reusing `MessageImage` (thumbnail, lightbox, copy / save / reveal), 300 px cap aligned to the label column, an olive `is-image` hue and a picture glyph; `inferToolCase('image')` → `generateImageToolCall` (standalone, never grouped — same as Cursor). **Legacy blocks** persisted before v35 (`toolKind: other`, path inside `output`) are recognised by the view (`legacyImageFromOutput`, same precedent as `legacyDiffFromOutput`), so the four cards that motivated this now show their images too. The inspector's 产物 tab lists generated images as a new source `process` (`processImageArtifacts`, deduplicated against images the reply text already references).
- **Not done**: `record_reply.files` is still stored but never projected to the timeline (dead parameter, separate decision); no placeholder skeleton while generating (the head verb carries the state); no live repro of whether the running cards were visible in real time — the code path shows them as `生成图片中 · 进行中`, and a follow-up generation should be watched once.
- **Verification**: typecheck, knip, full suite 176 files / 1769 tests, build, `smoke:channel`; new tests — hook (`generateImageToolCall` modern / running / legacy / failed shapes, base64 and the omitted-marker never in the frame), `parseProcessStream` (image only for image kind, blank dropped), `process-turn-view` (verbs, object, legacy recovery, no JSON output detail), `ProcessBlocks` render (thumbnail under the head, running card image-free, raw name gone), `DesktopSessionService` (path arrives with the frame and republishes), artifacts (new + legacy blocks, reply-reference dedupe). Preview scene `?image=1` with `session-process-image-{light,dark}` shots and a geometry probe (300 px cap, label-column alignment, no image body on the running card).

### App icon rebuilt as the "SG light-beam" monogram; icons become generated artifacts (2026-09-15)

- **Outcome**: the cream seal tile is replaced by the chosen V12 board artwork itself — a charcoal squircle, heavy uppercase `SG` in a deep orange gradient, one thin light blade crossing the tile's centre at ~28°. All four colour SVGs embed AI rasters: the big sizes (Dock PNG, `.icns` 128–1024, `.ico` 128/256, in-app brand mark) embed the original V12 board, the ≤ 64 px sizes embed a second AI image generated specifically for small sizes (flat colours, no glow, thicker blade); only the menu-bar template stays an exact-vector re-draw, because macOS demands pure black + alpha.
- **The vector redraw was measured, and still lost**: the first vector pass masked the beam inside the letter strokes; user feedback ("光条变短了、更差了") against the V12 board showed the light is the identity — a needle crossing the whole tile. Pixel-measuring V12 gave blade core ≈ 26 px, angle ≈ 27.9°, letter box ≈ 83 % of the tile, and the vector parameters (blade 27 × 920 px, taper 200, 28°; letters cap 430, G overlaps S 14 %, 32 px embolden) reproduce them — but the vector blade sits on the S/G junction, and with Futura's narrow S / wide G that junction is ~ 6.5 % left of the tile centre, while V12's blade passes dead centre on letterforms drawn for it. The user then chose AI art everywhere it can work (Dock first, then the small sizes too); `iconSvg` remains in `icon-lib.cjs` as the measured reference and still drives the tray template.
- **Raster embed geometry (all pixel-measured)**: `v12-master.png` (1152 × 864) carries the tile at x 88–688 / y 119–720 (≈ 601 px side); `v12-small.png` (1024²) is full-bleed (tile = the whole canvas); both are mapped edge-to-edge onto the 880 px `TILE_PATH` via `RASTER_SRC`. The AI tiles' corner arcs don't match the macOS squircle (the master's cuts up to ~ 27 px deeper), so clipping by `TILE_PATH` alone leaks the background at the corners: the image is clipped by a deeper-cornered round-rect (inset 6 px, rx 240) that stays inside the AI tile everywhere, and a flat `#141416` base tile fills what that clip removes — the corner regions are featureless dark, so the seam is invisible (verified: zero light pixels in all four 200 px corner zones of the 1024 render).
- **Design source is code, not a `.sketch`**: `docs/design/sg-monogram/icon-lib.cjs` builds both the raster-embed SVG (`rasterIconSvg`, source geometry in `RASTER_SRC`) and the vector SVG (`iconSvg`, parameters as above); `glyphs.json` holds the S / G outlines pulled once from macOS's Futura Bold (`extract-glyphs.cjs`, fontkit ad hoc) so the vector SVG embeds paths and depends on no font; `generate.cjs` writes the five `build/*.svg`. Re-running it reproduces the committed SVGs byte for byte.
- **Why five SVGs**: one source cannot be right at both 1024 and 16 px. The mac master (V12 embed) keeps the 86 % inset tile and drop shadow so the Dock weight matches native icons; `-small` (small-size AI embed, used ≤ 64 px) is flat and glow-free — measured at 16–64 px a downscaled master smears the blade into a haze while the flat art stays clean; its blade measures ~ 0.9 px at 16 px, fainter than the retired 70 px vector cut, accepted for letterform consistency with the Dock mark (user call); `-win` / `-win-small` scale the tile to 1.135 (≈ 98 % of the canvas, no shadow) because Windows icons are not inset; `trayTemplate.svg` is the mono vector silhouette fitted to 94 % of the canvas with a 3× blade gap so the 18 px template still shows the cut.
- **`scripts/render-icons.mjs` (`npm run icons`) rasterizes every size by laying the SVG out at that exact pixel size in headless Chrome / Edge and screenshotting** (same browser lookup as `preview-shots.mjs`; Node 24 built-in fetch / WebSocket, zero dependencies) — glow, beam and seam are recomputed per size instead of down-sampling 1024. Outputs: `icon-shiguang-1024.png` (Dock `setIcon`), `.icns` via `iconutil` from a temp iconset (16–1024, ten reps; macOS only), `.ico` with nine frames 16–256 (≤ 48 px stored as uncompressed 32-bit DIB + AND mask for the oldest decoders, larger as PNG), `trayTemplate.png/@2x` (18 / 36, pure black + alpha), and the in-app `brand-shiguang.png` (512 px full-bleed source for the 32 px top-bar mark). The run is deterministic: two runs give identical bytes.
- **Windows taskbar**: `win.icon` now points at the multi-size `.ico` instead of the 1024 PNG (electron-builder's PNG → ICO conversion down-sampled the inset mac tile, so the 16–32 px taskbar sizes were small and soft). The same `.ico` ships as `resources/tray.ico` (`win.extraResources`) and `createMacTray` became `createTray`: on Windows the notification-area icon is coloured (the system picks the 16 / 20 / 24 / 32 frame by DPI), on macOS it stays the template image; click / right-click behaviour is shared. Not run on a Windows machine yet — the NSIS job in CI validates that the `.ico` is accepted by rcedit / makensis; the tray itself is the user's check.
- **Not changed**: `dock-icon.png` extra resource and `setMacDockIcon()` keep working from the regenerated PNG; the in-app `BrandMark` still loads `./brand-shiguang.png` (32 px, `object-fit: cover`), only the asset changed.

### Usage ledger survives the run: per-row runId, archive instead of wipe (2026-09-15)

- **Symptom**: every 重新发起会话 / 组队 / 新一轮 emptied the stats page — 今天 / 7 天 / 30 天 all shrank to the current run, the 「历史会话」 group never appeared across runs. Not a rendering bug: two designs two weeks apart with contradictory premises. `8a70b48` (08-30) made the tracker + store **per-run** for the session-card badge (`cursorUsageRunDecision.reset` on a new `runId` → `sessions.clear()` + immediate `persist`, `cursor-usage-store` a single-`runId` file whose `load(runId)` returned `{}` on mismatch); `cea500f` (09-13) built the stats page on the same `CursorUsageSnapshot` assuming cross-run accumulation. Each of the three re-launch paths (`configureIndependentWorkspace` / `configureWorkspace` / `createNextRun`) mints `freshTeamRunKey()` → a new runId → the wipe. `stats-view.test.ts` fed mixed snapshots straight to the pure view model, so green tests hid the gap. Lost history is unrecoverable; accumulation starts from this build.
- **Rule**: ownership moves from the file to the row. `CursorSessionUsage.runId` (optional; absent on pre-migration rows and for trackers without run context) is stamped by the tracker (`stamp`) and carried through `projectUsage` / `reduceUsage` / `upgradeUsageEstimate` as a passenger label. `reset(nextRunId)` now **freezes** open ledgers (`frozenAt`, same marker `endActiveRun` uses, never rewritten on already-frozen rows), switches `this.runId`, prunes and persists — nothing is cleared. The badge's "old numbers vanish" is moved to the renderer: `usageBelongsToRun(usage, activeRun.id)` in `App` (rows without `runId` pass; the channel's `telemetryChannelComposerId` still points at the old composer until the new one binds, so the filter is load-bearing). `getSnapshot()` = current run + frozen history; `scheduleNotify` clones once and shares the object between persist and listeners.
- **Retention**: `USAGE_HISTORY_RETENTION_MS = 31 d` (stats max range is 30 d from local midnight, +1 d margin). Applied in `pruneHistory` at construction and at `reset`, only to rows whose `runId` differs from the current one — the live run is never pruned, whatever its age. Combined with the existing `MAX_SESSIONS = 1000` this bounds file size and the ~800 ms IPC push (rough worst case ≈ 3 MB at 5 busy seats × 30 d). Consequence for the page: 「累计」 now means "within the retained window", not all-time.
- **Store V4**: readable versions {2, 3, 4}; `load()` takes no `runId` and returns every row, stamping rows that lack `runId` with the file-level one (a V3 file was exactly one run's ledger, so the upgrade attributes it correctly and loses nothing); `save(runId, snapshot)` keeps writing the file-level `runId` as the diagnostic / fallback value, row `runId` is authoritative. Unknown versions → `{}` as before.
- **Composer reuse across runs** (theoretical: main's `usageComposerIds` filter only admits the current run's bindings): a frozen V3 ledger is un-frozen and continues under the new `runId` (1000 → 1500, one turn, no double count); a V2 row (no generation identity) is dropped and the new run starts an exact ledger, matching the pre-existing rule "V2 shows until the next run".
- **Verification**: `cursor-usage-tracker.test.ts` — reset archives (`runId 'a'`, `frozenAt` kept) while a new composer lands under `'b'`; freeze-on-reset + unfreeze-on-reuse; retention prunes 60 d rows of a past run but keeps a 2 d row and every current-run row, at reset and at startup; V2 replacement. `cursor-usage-store.test.ts` — mixed-run file round-trips per-row `runId`, untagged rows take the file-level one, ledgered rows keep it through projection, V3 upgrade, version 99 rejected. `cursor-usage-pipeline.test.ts` — restart within the same run (`runId: 'run'` on every tracker; the hook's fake clock starts at 1, so a run-less tracker would have pruned the row as 56-year-old history) and a `next-run` save that leaves the old row in place. `cursor-usage.test.ts` — `projectUsage` / `reduceUsage` / `upgradeUsageEstimate` passthrough, `usageBelongsToRun` matrix, retention ≥ 30 d.

### Session pool phase 2 · 2A: the orchestrator owns scheduling, groups own their plan policy (2026-09-16)

- **Why**: with groups usable (phase 1) the lead's briefing still asked it to chase members, schedule reviews and bounce rejected work, while the desktop orchestrator already did all of that — the 08-27 data (28 of 31 directives from the operator) said the lead had become an expensive inbox. Decision D1 makes the desktop the only scheduler; decision D2 (groups may have no lead) needed a real answer to "then who plans?".
- **Plan policy** (`team_groups.plan_policy`, additive column with default `'lead_only'`, guarded `ALTER` like `session_token`, no schema bump): `TeamGroup.planPolicy`, `defaultGroupPlanPolicy(leadSlotId)` (lead → `lead_only`, none → `any_member`), `groupMembersMayPlan(group)` (true only with no effective lead **and** `any_member`). Identity resolution overlays `LEAD_ROLE_CAPABILITIES` on every member of such a group (`effectiveCapabilities`, `IDENTITY_SELECT` now carries `g.plan_policy`), so `TaskAgentService.plan` / `listBoard` pass without a second permission path; `isEffectiveLead` stays false, so directive / broadcast / collect remain lead-only. Repository `setGroupPlanPolicy` (event `plan_policy_updated`), `TeamGroupService.setGroupPlanPolicy` (member `notice` only when `groupMembersMayPlan` flipped), IPC `team-group:set-plan-policy`, `CreateTeamGroupInput.planPolicy`. Pre-existing v8 groups are backfilled `lead_only` — an upgrade must not silently hand members planning rights.
- **The user plans from the desktop**: `TeamGroupService.planGroupTasks` → `TaskPoolService.planTasks(groupId, inputs)` → the same `pool.plan(runId, inputs, groupId)` the MCP uses; target-slot and capability checks mirror `TeamCollaborationAgentService.planTasks` so a task can never be created that nobody in the group could ever be dispatched. IPC `team-group:plan-tasks` (not gated by the session-launch guard: it only inserts rows). This is the one task write on the desktop surface; the dependency rule at the top was amended accordingly.
- **Lead notices reduced to three**: `TeamOrchestrator.reconcileStaleTasks` keeps the assignee reminder and drops the lead 【系统预警】; `TeamCollaborationSweeper.sweepUnanswered` keeps the sender receipt and drops the lead 【清扫提醒】 copy (an operator-sent stale directive therefore produces nothing). New: `TaskDispatcher.notifyOutcome` — 【任务完成】 with the delivery summary / 【任务失败】 with the reason after the last attempt, to the task's group lead, idempotent per `taskId:status:attemptCount` (in-memory set so terminal tasks are not re-queried every tick, repository `clientMessageId` for restarts; `cancelled` is not notified). New: `TeamCollaborationSweeper.sweepMemberAttention` — 【成员离线提醒】 to the group lead when a member's runtime has `hasConfirmedRuntimeStop` (same evidence as `TeamGroupView.attention`), once per stop cycle, reset on recovery, never for the lead itself (that is the lead-heartbeat broadcast) and never in a lead-less group.
- **Briefings** (`buildTeamRoleBriefing`): lead = plan on explicit user request + answer the user + relay real reports; the sentences about chasing, scheduling review and bouncing work are gone, "禁止代答" stays. Members: claim / progress / submit / fail / review already report through the tool actions — no hand-written `team_message` copy ("静默干活即失职" removed). `TeamRoleBriefingGroup.membersMayPlan` drives the lead-less wording (user creates tasks, or any member plans) and adds the plan rule to a member's step 2 when it applies. `team_task`'s description no longer says "plan（主控专用）".
- **Verification**: `team-groups-repository` (+2: policy defaults / overlay / `plan_policy_updated`; column added to a v8 DB that predates it), `team-group-service` (+2: policy change gates `plan` and briefs members only on a real change; `planGroupTasks` scope / capability / key / lifecycle checks and dispatcher pickup), `register-team-group-ipc` (policy + task-list shape validation, plan-tasks not blocked by the launch guard), `team-orchestration-groups` (stale task → assignee only; done / failed → one lead notice each, progress → none, restart-safe), `team-collaboration-sweeper` (+2: attention once per cycle / reset / lead excluded / lead-less silent / suspected ≠ stopped; unanswered → sender only), `team-control-domain` (briefing contract for lead / member / lead-less any_member / lead_only). Full suite 1930 tests; typecheck clean.

### Session pool phase 2 · 2B-1: the team run's creation and launch paths are retired (2026-09-16)

- **Why**: after 2A the one-shot team run had nothing left that a collaboration group does not do better, but it still owned the biggest state machine in the code base — `draft → ready → launching → running` with per-binding `launchStatus`, a run-level goal, a launch prompt that promised "已启动", a self-heal inside `recordAgentCheckIn`, and a mode switch on the 运行 page that let a user create one by accident. The 09-13 data said the path was dead (18 draft / 10 ready never launched, 26 completed). This step deletes every way to *reach* that machine; collapsing the enums it left behind is 2B-2.
- **Schema v9** (`SqliteTeamControlRepository.migrate`, one transaction, idempotent): every non-`completed` legacy team run (`template_id ≠ independent-session-v1`) becomes `completed` and its bindings `failed` with `archived: legacy team run（升级归档，团队 run 已退役）` — the fence then answers `run_completed` to any session still holding one of its tokens, which is the only way an upgrade can tell those sessions to stop; a `running` pool run that is no longer the newest run of its workspace (left behind by early builds) is archived too; finally any remaining `draft / ready / launching / attention / paused` row is forced to `completed`, so `status` has two values from here on. `runtime_bindings.launch_status` stays as a column (`not_started` / `acknowledged` / `failed` still occur) for old builds sharing the file.
- **Repository**: `updateRunGoal`, `beginLaunch`, `ensureRunLaunching`, `recordLaunchDelivery` and the `ready → draft` repair at the end of `migrate()` are gone; `recordAgentCheckIn` no longer heals a run to `launching` or promotes it to `running` on the last acknowledgement — it records the check-in and refuses a non-running run with `run_completed`; `completeRun` / `setActingLead` act only on `running`; the default completion detail is the neutral 本轮运行已结束.
- **Service**: `configureIndependentWorkspace` is `createSessionPool` (replace the current run, whatever its kind, through `replaceActiveRun`); `ensureWorkspace`, `configureWorkspace`, `createNextRun`, `updateGoal`, `launch`, `ensureRunLaunched`, `settleAgentSessionLaunch`, `transferLead`, `clearActingLead`, the launch-in-flight guard and the legacy pre-launch freshening are deleted, and with them the confirmation-timeout constructor argument. `AgentSessionLauncher.onFinished` is a plain completion hook (nothing to settle any more).
- **Launch prompt**: `buildTeamLaunchHint` is deleted; every seat, grouped or not, opens with the solo `check_messages` prompt (`team-agent-launch-prompts.ts` has no run-mode branch). A rebuilt grouped seat learns about its group from the membership notice on its first poll — the same path stage 0 validated — which is what 2C builds on.
- **IPC / API / MCP**: `team-control:prepare-detected-workspace`, `choose-workspace`, `create-team`, `next-run`, `prepare-active-setup`, `update-goal`, `launch` and the `TeamSetupDraft` / `CreateTeamInput` / `ChooseTeamWorkspaceResult` types are removed from `desktop-api.ts` and the preload; `resolveTeamSetupMembers` is gone from `team-setup.ts` (model validation lives only in `resolveIndependentSessionMembers`). `team_run start` answers `not_applicable` unconditionally (the action itself goes in phase 4); `transfer_lead` requires `running`.
- **Renderer**: `TeamSetupPage`, `RunModeSwitch`, `RunTeamPanel`, `team-setup.css`, `team-skill-defaults` and the `TeamIcon` / `SoloIcon` exports are deleted; `run-view.ts` has no `mode` / `presence` / `gates` and three phases (`none` / `active` / `completed`); an archived legacy team run that is still the workspace's newest run is projected as `phase: none` + `archivedLegacyTeam`, so the page shows the start page with one line (`.run-start__note`) instead of a dead team dashboard; the dead team-flow CSS (mode switch, step connectors, goal editor, gates) is gone and `theme-surface-contract` now asserts its absence. Preview scenes and both smoke scripts (`verify-built-mcp`, `verify-channel-mcp`) are built around a pool plus a group.
- **Tests**: `createDefaultTeamBundle` moved to `tests/legacy-team-fixtures.ts` (a legacy team run is a test-only object now; the fixture writes it `running` with an optional `goal`, ordered by channel number like the old domain helper). `team-control-service` and `team-setup` were rewritten for the pool; `sqlite-team-control-repository` covers check-in without a launch machine (acknowledged stays, run stays running, `run_completed` on an ended run, same-topology re-install keeps generation and token) and the v9 migration (archive / superseded pool / fence / idempotent, `revision` unchanged on the second open); `run-view`, `run-page`, `solo-five-seat` e2e (pool + group), `team-agent-launch-prompts`, `team-failover`, `team-group-service` and the collaboration / orchestrator / memory / continuity fixtures follow. Full suite 1884 tests (`brand-migration` still the known local `node:sqlite` environment failure); typecheck, knip, build and both smokes clean.

### Session pool phase 2 · 2B-2: the launch-era enums collapse to the two-state run (2026-09-16)

- **Why**: 2B-1 deleted every way to *reach* the launch state machine but left its vocabulary in the types — five dead `TeamRunStatus` values, a `TeamLaunchStatus` on every binding, a `launching` readiness, three preflight launch gates — and a failover service whose main body (all-offline completion, standby takeover, lead auto-transfer) presupposed the one-shot team run those enums described. Every consumer still carried branches for states that could no longer occur.
- **Domain**: `TeamRunStatus = 'running' | 'completed'` — a run is running from the moment it exists; ended / replaced / archived means completed; attention lives on the group (`TeamGroupView.attention`), pause does not exist. Deleted: `TeamLaunchStatus`, `RuntimeBinding.launchStatus / launchCommandId`, `TeamRun.launchedAt`, readiness `launching` / `attention`, `TeamPreflight.goalDefined / agentsWaiting / canLaunch`. `launchDetail` stays as the binding note (run-close reason / handoff note; the column name is historical). `acknowledgedAt` is the one "this session checked in" evidence: readiness `active` = it is set, and `recordAgentCheckIn` now writes `COALESCE(acknowledged_at, ?)` so the **first** check-in sticks — before, every repeat check-in reset it, and with it the projected session start time; repeats only refresh `last_check_in_*`. Dead exports whose only callers were launch-era code go too: `ALL_TEAM_CAPABILITIES`, `hasOpenReplySync` (the all-offline completion gate was its last caller).
- **v9 amended before it ships** (the production file is still v8, so the migration could be changed in place): archiving a run is now `completeRun`'s three writes — status `completed`, **revoke that run's agent registrations** (per-run `UPDATE`, not the workspace-wide helper, so the surviving pool is untouched), note into `launch_detail` — applied to legacy team runs (`archived: legacy team run…`) and to superseded stale pool runs (new note `archived: superseded pool run（升级归档，已被更新的运行取代）`; 2B-1 archived those silently). Without the revocation, an archived run that is still its workspace's newest kept resolving agent identities whose every `team_*` call then died at the run gate; with it the session is fenced exactly like any ended run. The migration ends by normalizing `launch_status = 'not_started'`, `launch_command_id = NULL` across all bindings — the column survives for old builds sharing the file but never carries state again (2B-1's `failed` markers included: the fence keys on `run_status`, never on `launch_status`).
- **Failover service reduced to its real residue** (345 → 89 lines): subscribe to the team snapshot — subscribe replays, so `start()` repairs a run that completed while the desktop was closed — and when the active run is `completed`, cancel its open tasks once (`RUN_CLOSED_TASK_REASON` = 会话池已结束…, per-run `closedRuns` set; a failed `closeRun` reports through the new `onerror` option and is retried at the next snapshot instead of being marked closed). All-offline completion, `reconcileAcknowledgements`, standby auto-takeover and lead auto-promotion are deleted with their grace-period options; `TeamHandoffService.automatic` goes with them. The manual-handoff facade stays for 2C to turn into `transferMembership` — in production every run is a pool, so `options()` refuses today (`handoff_pool_run` / `handoff_run_inactive`).
- **Consumers**: readiness `active` reads `acknowledgedAt` (was `launchStatus === 'acknowledged'`) in `TeamControlService.readinessOf` and the handoff blockers; `verifyAgentRuntime` drops `requiresCursorEvidence` — with two run states it was constant true, so every bound channel is always checked against Cursor evidence; usage collecting is `status === 'running'`; the collaboration / task-pool write gates refuse only `completed`; the telemetry bindings fingerprint drops `launchStatus`; `upsertWorkspaceTeam` no longer resets a changed topology to `draft` and `recordInstallation` no longer promotes `draft ↔ ready` (both leave run status alone). The MCP briefing exists only for grouped seats (`buildTeamRoleBriefing.group` is required; the run-level lead branch was unreachable once v9 revokes archived runs' registrations). Renderer: `team-dashboard-view.ts` deleted with its test (nothing else imported it since 2B-1), `App.tsx` loses the `reviving` mapping, `handoff-entry` / `team-collaboration-view` / preview scenes are two-state.
- **Verification**: `team-failover.test.ts` rewritten 1069 → 291 lines around the reduced service (pool never completed on any offline combination — invariant I5; close-once with the pool reason; `start()` replay repair; `onerror` + retry; `handoff_pool_run` / `handoff_run_inactive`); the repository suite asserts the amended v9 (registrations revoked for archived runs, stale-pool note, `launch_status` / `launch_command_id` normalized — checked via raw SQL since the domain no longer projects them, fence `run_completed`, check-in refused, idempotent reopen) and first-check-in-sticks; ~30 test files drop `launchStatus` / `goalDefined` / `agentsWaiting` / `canLaunch` and the old status literals, `team-dashboard-view.test.ts` deleted. Full suite 189 files / 1859 tests (`brand-migration` = the known local Node 22 `node:sqlite` environment failure); typecheck, knip, build, `smoke:channel` and `smoke:mcp` clean.

### Session pool phase 2 · 2C: seat rebuild and handoff learn about groups (2026-09-17)

- **Why**: two leftovers of the one-shot team run. (1) `prepareComposerRelaunch` keeps the seat's `group_id / role_id` (phase 1), but the session that binds afterwards starts from the solo launch hint and the group's `joined` notice was consumed by its predecessor — it is *authorized* as a group member while *believing* it is independent. (2) `TeamHandoffService.manualHandoff` moved the **binding** (channel + token, `composer_id` cleared) onto a standby or donor seat — the remedy for a one-shot seat whose Agent died. In the pool channel = seat and the binding *is* the session; what an offline grouped seat needs is for its **group identity** to move.
- **Rebuild notice**: `TeamControlService.recordComposerBinding`. The repository writes only while `composer_id IS NULL` (first binding wins; a repeat report or a rebind attempt returns `false`), so `changed` is exactly the seat's "no Composer → Composer" transition — no before/after diffing needed. When that seat is in an active group the service writes `member_rejoined_after_rebuild` (`recordGroupMemberRebuilt`: actor `system`, detail = group role name, revision bump) and sends a `joined` membership notice with the current lead label (`groupLeadLabel`, now a domain helper shared with `TeamGroupService`); fail-soft through a new `TeamControlService` `onerror` option. A seat grouped *while unbound* may see the notice twice (join-time and bind-time); it is idempotent ("call `team_check_in`") and `check_messages` delivers in order, so this was accepted over a persistent "was rebuilt" flag. Solo launch hint unchanged; seat auto-rotation stays solo-only (`seatRotationVerdict` → `not_solo`) — the notice makes grouped rotation *possible*, it does not enable it.
- **Membership transfer** replaces manual handoff. Repository `transferGroupMembership({ groupId, fromSlotId, toSlotId })`, one `BEGIN IMMEDIATE`: `leaveMember(A)` (group role row deleted, home role restored, `member_left`) → `joinMembers([B], A's role template)` (B gets the same `role_key`; a grouped / foreign / identical target is refused with the existing codes and nothing is written) → every lead pointer at A (`lead_slot_id`, `acting_lead_slot_id`) re-aimed at B with `lead_changed` / `acting_lead_changed` events (`A → B（成员身份迁移）`) → audit row in `team_failovers` (`reason = manual_membership_transfer`, `status = completed` on write, from / to channel and session ids; nothing to wait for). Returns `{ group, from, to, transferredLead, failover }` where `transferredLead` means the **effective** lead was A (a nominal lead with a different acting lead: pointer moves, `transferredLead` false, nobody is told they became lead). Bindings, tokens, Composers and `generation` are untouched (asserted before / after). `TeamGroupService.transferMembership` then runs the same post-transaction settlement as `removeGroupMember` (shared `settleDeparture`: `releaseAgentWork` with `member_left`, `orphanPendingReceipts`) and notifies: A `left`, B `joined` (lead label after the move), B additionally `lead_changed(becameLead)` when the effective lead moved. Deviation from the task book's "old and new lead both get `lead_changed`": A has left the group and that template tells its reader to `team_check_in` (which would now answer `not_in_group`) — A's `left` notice already says it holds nothing. A third-party effective lead gets one `notice` (成员身份已迁移 + the shared departure follow-up). `membershipTransferOptions(slotId)`: source must be in an active group (`transfer_source_not_grouped`), candidates = every `solo` seat of the pool — online or offline, an offline target just queues the notice and the context document — `transfersLead` = source is the effective lead.
- **Removed**: `TeamHandoffService` (270 lines), repository `rebindSlotToStandby / rebindSlotFromMember / attachFailoverContext / updateFailoverStatus` (~330 lines), `TeamFailoverRebindResult`, `TeamContinuityService.createTakeoverCapsule` + `TeamTakeoverCapsule` (`roleRestoreContent` is restore-only), the `TeamHandoffCandidate / TeamHandoffOptions / ManualTeamHandoff*` types (→ `MembershipTransferCandidate / Options / Input / Result / Outcome`). `TeamFailoverService` keeps only the run-close repair (constructor = team + tasks + `onerror`). `TeamFailoverStatus` keeps `waiting_for_agent` / `failed` for historical rows.
- **Orchestration / IPC / renderer**: `manualHandoffWithContext` → `transferMembershipWithContext`, same order — resolve the source context first, not because the binding moves any more but so a failed transfer leaves no delivery side effect and a failed resolution never blocks the transfer; the target channel comes from the result, falling back to the pre-transfer snapshot. IPC channel names unchanged (`team-continuity:handoff-options` / `team-continuity:handoff`); the handler validates `groupId / fromSlotId / toSlotId / includeContext` and is wired to `TeamGroupService`; API / preload `getMembershipTransferOptions` / `transferMembership`. `ManualHandoffDialog` (file name kept; phase 3 redoes the UI) lists every independent seat with an online / offline tag and no eligibility gate; the result page shows the role, the target channel, whether the lead moved, released tasks and the context result; `App.tsx` selects `transfer.toChannelId`; `handoff-entry` keeps its three states with `roles` reworded; preview `?handoff=1` mock rebuilt around groups.
- **Verification**: `team-control-service` (+1: rebuild sequence — first binds of ungrouped seats silent; grouped seat relaunch → exactly one `joined` with lead label and `team_check_in`, event `member_rejoined_after_rebuild`, `group_id` kept, token rotated; repeat report / rebind attempt return `false` and add nothing; independent seat relaunch silent), `team-groups-repository` (+4: transaction sweep with bindings compared before / after and `member_left → member_joined` order; lead / acting-lead carry including nominal-lead-with-acting-lead → `transferredLead: false`; refusals atomic — grouped target leaves A in place, foreign source, dissolved group, ended pool, no failover row; `recordGroupMemberRebuilt` event / revision / non-member / dissolved), `team-group-service` (+3: lease release + task back to `queued` + identity switch + notices + lead notice; lead carry → `left / joined / lead_changed(B)` and nothing to a third party; options: grouped source only, solo candidates, `group_run_inactive` after end), `manual-handoff-with-context` rewritten for the transfer ports (5 cases incl. target-channel fallback), `manual-handoff-dialog` / `app-handoff-entry` for the new dialog, `team-failover` drops the facade cases. Full suite 189 files / 1866 tests (1865 passed + 1 skipped; `brand-migration` = the known local Node 22 `node:sqlite` environment failure); typecheck, knip, build, `smoke:channel` and `smoke:mcp` clean.

### Session pool phase 2 · 2D: team continuity is retired instead of taught about groups (2026-09-17)

- **Why**: the task book (written by the 09-13 read-only review) assumed one-click recovery existed and asked 2D to make it restore group structure. Reading before writing showed that `TeamContinuityService.restore()` had **no production caller** — none in the code, none in the git history: the renderer's snapshot IPC was removed on 08-30 with a comment deferring the read to "the restore flow in the main process", which was never wired, and the only production reader of a checkpoint was the old failover's `createTakeoverCapsule`, deleted in 2C. What was left was a write-only log: `capture()` on every task / message / memory change (650 ms debounce, content digest, 100 per run, ≤ 512 KB each) that nothing read. In the pool model there is also no scenario for a team-wide snapshot: the pool does not end by accident, the unit of failure is one session, and the recovery chain (rebuild + context handoff + rebind notice + `team_check_in`) reads live state — see "Recovery in the pool model". Options put to the user: (A) retire the module, (B) keep the auto-checkpoint as a write-only record, (C) implement the task book plus a new UI entry; the user chose A.
- **Removed** (~1000 lines): `TeamContinuityService` (332), `TeamContinuityRepository` + `SqliteTeamContinuityRepository` (31 + 247), `domain/team-continuity.ts` (113), `registerTeamContinuityIpc` (51), `tests/team-continuity.test.ts` (201, 2 cases), the preview `continuitySnapshot` mock; `main/index.ts` no longer builds or disposes the fourth repository and the capture watcher. The `TeamContinuitySnapshot` import in `desktop-api.ts` was already unused.
- **Storage**: the four tables (`team_continuity_meta`, `team_checkpoints`, `team_restore_operations`, `team_restore_members`) are dropped at the end of `SqliteTeamControlRepository.migrate()`, child → parent, guarded by `sqlite_master` existence and `DROP TABLE IF EXISTS`, so it is idempotent across the desktop / MCP processes and a no-op on a database that never had them. No schema-version bump (same rule as the additive columns: an older build sharing the file just recreates them on open and the next new-build open drops them again). The test seeds the old tables with rows and foreign keys and checks the current tables and data are untouched.
- **IPC moved home**: the two membership-transfer channels lived in the continuity IPC file only because that is where the old handoff lived. They are `team-group:transfer-options` / `team-group:transfer-membership` now, registered by `registerTeamGroupIpc` (new positional `transferContext` port: `team` snapshot + `handoff`), validated with the file's `requiredString` / `objectOf` helpers, and `transfer-membership` is behind the same `assertNoSessionLaunch` guard as every other membership change (`transfer-options` is a read and is not). `manual-handoff-with-context.ts` → `membership-transfer-with-context.ts` (file name follows the 2C rename; content unchanged). README no longer advertises 自动检查点 / 一键恢复.
- **Verification**: `register-team-group-ipc` covers the two transfer handlers (options normalization, transfer with / without context — the failed context resolution is reported in `contextHandoff`, not thrown — shape refusals, launch guard, dispose), `sqlite-team-control-repository` (+1: retired tables dropped with data, live run / binding intact, second open no-op). Full suite 188 files / 1865 tests (1864 passed + 1 skipped; `brand-migration` = the known local Node 22 `node:sqlite` environment failure); typecheck, knip, build, `smoke:channel` and `smoke:mcp` clean.
